# 第19章 工作树

> 原始页面：[Worktrees – Codex app | OpenAI Developers](https://developers.openai.com/codex/app/worktrees)

## 本章目标
这章面向会 Python、懂一点 LangChain、但还没有真正把 AI Agent 当成工程系统来使用的读者。重点不是背术语，而是把“这个功能为什么存在、什么时候该用、怎么安全地用”讲清楚。

## 先用一个高中数学类比
把自动化看成数列中的递推过程。你先给出初值和递推规则，之后系统会在每个时刻自动算出下一项。

## 严谨定义
严格地说，自动化就是一个由“触发条件 + 指令 + 执行环境 + 输出汇总”组成的重复映射。

## 你读完应该抓住什么
- 在 Codex 应用中，工作树使 Codex 能够在同一个项目中运行多个独立任务，而不会相互干扰。对于 Git 仓库，自动化 在专用后台工作树上。
- `什么是工作树`：工作树仅在 Git 仓库中的项目中有效，因为它们在底层使用 Git 工作树。工作树允许您创建仓库的第二个副本（“检出”）。每个。
- `术语`：- **本地检出**：您创建的仓库。在 Codex 应用中有时仅称为 **本地**。

## 分步理解
### 正文
先说直白版：
在 Codex 应用中，工作树使 Codex 能够在同一个项目中运行多个独立任务，而不会相互干扰。对于 Git 仓库，自动化 在专用后台工作树上运行，以免与您正在进行的工作发生冲突。在非版本控制的项目中，自动化直接在项目目录中运行。您还可以手动在工作树上启动线程，并使用 Handoff 将线程在本地和工作树之间移动。

把它理解成一个更严谨的过程：
1. 在 Codex 应用中，工作树使 Codex 能够在同一个项目中运行多个独立任务，而不会相互干扰。对于 Git 仓库，自动化 在专用后台工作树上运行，以免与您正在进行的工作发生冲突。在非版本控制的项目中，自动化直接在项目目录中运行。您还可以手动在工作树上启动线程，并使用 Handoff 将线程在本地和工作树之间移动。

### 什么是工作树
先说直白版：
工作树仅在 Git 仓库中的项目中有效，因为它们在底层使用 Git 工作树。工作树允许您创建仓库的第二个副本（“检出”）。每个工作树都有您仓库中每个文件的独立副本，但它们都共享有关提交、分支等相同的元数据（`.git` 文件夹）。这使您能够并行检出并在多个分支上工作。

把它理解成一个更严谨的过程：
1. 工作树仅在 Git 仓库中的项目中有效，因为它们在底层使用 Git 工作树。工作树允许您创建仓库的第二个副本（“检出”）。每个工作树都有您仓库中每个文件的独立副本，但它们都共享有关提交、分支等相同的元数据（`.git` 文件夹）。这使您能够并行检出并在多个分支上工作。

### 术语
先说直白版：
- **本地检出**：您创建的仓库。在 Codex 应用中有时仅称为 **本地**。
- **工作树**：从您在 Codex 应用中本地检出创建的 Git 工作树。
- **Handoff**：将线程在本地和工作树之间移动的流程。Codex 处理所需的 Git 操作，以安全地在它们之间移动您的工作。

把它理解成一个更严谨的过程：
1. **本地检出**：您创建的仓库。在 Codex 应用中有时仅称为 **本地**。
2. **工作树**：从您在 Codex 应用中本地检出创建的 Git 工作树。
3. **Handoff**：将线程在本地和工作树之间移动的流程。Codex 处理所需的 Git 操作，以安全地在它们之间移动您的工作。

### 为什么使用工作树
先说直白版：
1. 与 Codex 并行工作，而不会干扰您当前的本地设置。 2. 在您专注于前台的同时排队后台工作。 3. 当您准备好检查、测试或更直接地协作时，将线程移动到本地。

把它理解成一个更严谨的过程：
1. 1. 与 Codex 并行工作，而不会干扰您当前的本地设置。 2. 在您专注于前台的同时排队后台工作。 3. 当您准备好检查、测试或更直接地协作时，将线程移动到本地。

### 入门
先说直白版：
工作树需要一个 Git 仓库。确保您选择的项目位于其中。
1. 选择“工作树”
在新线程视图中，在撰写器下选择 **工作树**。 可选地，选择一个 本地环境 来运行工作树的设置脚本。
2. 选择起始分支

把它理解成一个更严谨的过程：
1. 工作树需要一个 Git 仓库。确保您选择的项目位于其中。
2. 1. 选择“工作树”
3. 在新线程视图中，在撰写器下选择 **工作树**。 可选地，选择一个 本地环境 来运行工作树的设置脚本。
4. 2. 选择起始分支

## 实战视角
如果你会 Python，可以把这一章里的能力理解成一个可以读文件、调工具、保留状态、按规则执行的程序代理。它和 LangChain 的链、工具、记忆很像，但 Codex 更强调真实工程环境：仓库、命令、审阅、权限、并行任务。

## 给高中水平读者的最后一句话
不要急着一次学完整个体系。把每章都当成“定义 + 例子 + 适用边界”三件事去掌握，AI Agent 就会从模糊概念变成你能实际操作的工程对象。
