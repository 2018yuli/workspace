# 第23章 命令

> 原始页面：[Commands – Codex app | OpenAI Developers](https://developers.openai.com/codex/app/commands)

## 本章目标
这章面向会 Python、懂一点 LangChain、但还没有真正把 AI Agent 当成工程系统来使用的读者。重点不是背术语，而是把“这个功能为什么存在、什么时候该用、怎么安全地用”讲清楚。

## 先用一个高中数学类比
线程像解一道多步函数题时保留下来的草稿纸。后一步是否顺利，依赖前面保留下来的中间结果。

## 严谨定义
严格地说，线程是一个按时间顺序累积状态的信息序列。

## 你读完应该抓住什么
- 使用这些命令和键盘快捷键来导航 Codex 应用。
- `键盘快捷键`：| | 操作 | macOS 快捷键 | | --- | --- | --- | | **通用** | | | | | 命令菜单。
- `斜杠命令`：斜杠命令让您在不离开线程编辑器的情况下控制 Codex。可用的命令根据您的环境和访问权限而异。

## 分步理解
### 正文
先说直白版：
使用这些命令和键盘快捷键来导航 Codex 应用。

把它理解成一个更严谨的过程：
1. 使用这些命令和键盘快捷键来导航 Codex 应用。

### 键盘快捷键
先说直白版：
| | 操作 | macOS 快捷键 | | --- | --- | --- | | **通用** | | | | | 命令菜单 | `Cmd` \+ `Shift` \+ `P` 或 `Cmd` \+ `K` | | | 设置 | `Cmd` \+ `,` | | | 打开文件夹 | `Cmd` \+ `O` | | | 向后导航 | `Cmd` \+ `[` |\ | | 向前导航 | `Cmd` \+ `]` | | | 增加字体大小 | `Cmd` \+ `+` 或 `Cmd` \+ `=` | | | 减小字体大小 | `Cmd` \+ `-` 或 `Cmd` \+ `_` | | | 切换侧边栏 | `Cmd` \+ `B` | | | 切换差异面板 | `Cmd` \+ `Option` \+ `B` | | | 切换终端 | `Cmd` \+ `J` | | | 清空终端 | `Ctrl` \+ `L` | | **线程** | | | | | 新线程 | `Cmd` \+ `N` 或 `Cmd` \+ `Shift` \+ `O` | | | 在线程中查找 | `Cmd` \+ `F` | | | 上一个线程 | `Cmd` \+ `Shift` \+ `[` |\ | | 下一个线程 | `Cmd` \+ `Shift` \+ `]` | | | 语音输入 | `Ctrl` \+ `M` |

把它理解成一个更严谨的过程：
1. | | 操作 | macOS 快捷键 | | --- | --- | --- | | **通用** | | | | | 命令菜单 | `Cmd` \+ `Shift` \+ `P` 或 `Cmd` \+ `K` | | | 设置 | `Cmd` \+ `,` | | | 打开文件夹 | `Cmd` \+ `O` | | | 向后导航 | `Cmd` \+ `[` |\ | | 向前导航 | `Cmd` \+ `]` | | | 增加字体大小 | `Cmd` \+ `+` 或 `Cmd` \+ `=` | | | 减小字体大小 | `Cmd` \+ `-` 或 `Cmd` \+ `_` | | | 切换侧边栏 | `Cmd` \+ `B` | | | 切换差异面板 | `Cmd` \+ `Option` \+ `B` | | | 切换终端 | `Cmd` \+ `J` | | | 清空终端 | `Ctrl` \+ `L` | | **线程** | | | | | 新线程 | `Cmd` \+ `N` 或 `Cmd` \+ `Shift` \+ `O` | | | 在线程中查找 | `Cmd` \+ `F` | | | 上一个线程 | `Cmd` \+ `Shift` \+ `[` |\ | | 下一个线程 | `Cmd` \+ `Shift` \+ `]` | | | 语音输入 | `Ctrl` \+ `M` |

### 斜杠命令
先说直白版：
斜杠命令让您在不离开线程编辑器的情况下控制 Codex。可用的命令根据您的环境和访问权限而异。

把它理解成一个更严谨的过程：
1. 斜杠命令让您在不离开线程编辑器的情况下控制 Codex。可用的命令根据您的环境和访问权限而异。

### 使用斜杠命令
先说直白版：
1. 在线程编辑器中输入 `/`。 2. 从列表中选择一个命令，或继续输入进行过滤（例如，`/status`）。
您还可以通过在线程编辑器中输入 `$` 明确调用技能。请参见 技能。
启用的技能也会出现在斜杠命令列表中。

把它理解成一个更严谨的过程：
1. 1. 在线程编辑器中输入 `/`。 2. 从列表中选择一个命令，或继续输入进行过滤（例如，`/status`）。
2. 您还可以通过在线程编辑器中输入 `$` 明确调用技能。请参见 技能。
3. 启用的技能也会出现在斜杠命令列表中。

### 可用的斜杠命令
先说直白版：
| 斜杠命令 | 描述 | | --- | --- | | `/feedback` | 打开反馈对话框提交反馈，并可选择性地包括日志。 | | `/mcp` | 打开 MCP 状态以查看连接的服务器。 | | `/plan-mode` | 切换多步计划的计划模式。 | | `/review` | 开始代码审查模式，审查未提交的更改或与基础分支比较。 | | `/status` | 显示线程 ID、上下文使用情况和速率限制。 |

把它理解成一个更严谨的过程：
1. | 斜杠命令 | 描述 | | --- | --- | | `/feedback` | 打开反馈对话框提交反馈，并可选择性地包括日志。 | | `/mcp` | 打开 MCP 状态以查看连接的服务器。 | | `/plan-mode` | 切换多步计划的计划模式。 | | `/review` | 开始代码审查模式，审查未提交的更改或与基础分支比较。 | | `/status` | 显示线程 ID、上下文使用情况和速率限制。 |

## 实战视角
如果你会 Python，可以把这一章里的能力理解成一个可以读文件、调工具、保留状态、按规则执行的程序代理。它和 LangChain 的链、工具、记忆很像，但 Codex 更强调真实工程环境：仓库、命令、审阅、权限、并行任务。

## 给高中水平读者的最后一句话
不要急着一次学完整个体系。把每章都当成“定义 + 例子 + 适用边界”三件事去掌握，AI Agent 就会从模糊概念变成你能实际操作的工程对象。
