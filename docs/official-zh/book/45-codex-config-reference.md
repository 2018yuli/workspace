# 第45章 配置参考

> 原始页面：[Configuration Reference – Codex | OpenAI Developers](https://developers.openai.com/codex/config-reference)

## 本章目标
这章面向会 Python、懂一点 LangChain、但还没有真正把 AI Agent 当成工程系统来使用的读者。重点不是背术语，而是把“这个功能为什么存在、什么时候该用、怎么安全地用”讲清楚。

## 先用一个高中数学类比
沙盒像在平面直角坐标系里画出的一个有边界的定义域。函数只能在定义域内取值，超出范围就不允许。

## 严谨定义
严格地说，沙盒是执行权限、文件范围和外部访问能力的约束集合。

## 你读完应该抓住什么
- 使用此页面作为 Codex 配置文件的可搜索参考。有关概念指导和示例，请从 配置基础 和 高级配置 开始。
- ``config.toml``：用户级配置保存在 `~/.codex/config.toml` 中。您还可以在 `.codex/config.t。

## 分步理解
### 正文
先说直白版：
使用此页面作为 Codex 配置文件的可搜索参考。有关概念指导和示例，请从 配置基础 和 高级配置 开始。

把它理解成一个更严谨的过程：
1. 使用此页面作为 Codex 配置文件的可搜索参考。有关概念指导和示例，请从 配置基础 和 高级配置 开始。

### `config.toml`
先说直白版：
用户级配置保存在 `~/.codex/config.toml` 中。您还可以在 `.codex/config.toml` 文件中添加项目范围的覆盖。Codex 仅在您信任该项目时加载项目范围的配置文件。
对于沙盒和批准密钥 (`approval_policy`、`sandbox_mode` 和 `sandbox_workspace_write.*`)，请将此参考与 沙盒和批准、可写根中的受保护路径 和 网络访问 一起使用。
| 键 | 类型 / 值 | 详情 | | --- | --- | --- | | `agents.<name>.config_file` | `string (path)` | 此角色的 TOML 配置层的路径；相对路径以声明角色的配置文件为基准解析。 | | `agents.<name>.description` | `string` | 在选择和产生该代理类型时，显示给 Codex 的角色指导。 | | `agents.<name>.nickname_candidates` | `array<string>` | 可选的显示昵称池，用于以该角色产生的代理。 | | `agents.job_max_runtime_seconds` | `number` | `spawn_agents_on_csv` 作业的每个工作者的默认超时。当未设置时，工具会回退到每个工作者 1800 秒。 | | `agents.max_depth` | `number` | 允许为产生的代理线程最大嵌套深度（根会话的深度为 0；默认值：1）。 | | `agents.max_threads` | `number` | 可以同时打开的代理线程的最大数量。当未设置时，默认为 `6`。 | | `allow_login_shell` | `boolean` | 允许基于 shell 的工具使用登录 shell 语义。默认值为 `true`；当为 `false` 时，`login = true` 请求被拒绝，而省略 `login` 默认使用非登录 shell。 | | `analytics.enabled` | `boolean` | 启用或禁用此机器/配置文件的分析功能。当未设置时，客户端默认应用。 | | `approval_policy` | `untrusted | on-request | never | { granular = { sandbox_approval = bool, rules = bool, mcp_elicitations = bool, request_permissions = bool, skill_approval = bool } }` | 控制 Codex 在执行命令之前何时暂停以进行批准。您还可以使用 `approval_policy = { granular = { ... } }` 允许或自动拒绝特定提示类别，同时保持其他提示的互动性。`on-failure` 已被弃用；对交互式运行使用 `on-request`，或对非交互式运行使用 `never`。 | | `approval_policy.granular.mcp_elicitations` | `boolean` | 当为 `true` 时，允许 MCP 引导提示浮现，而不是被自动拒绝。 | | `approval_policy.granular.request_permissions` | `boolean` | 当为 `true` 时，允许来自 `request_permissions` 工具的提示浮现。 | | `approval_policy.granular.rules` | `boolean` | 当为 `true` 时，允许由 execpolicy 的 `prompt` 规则触发的批准浮现。 | | `approval_policy.granular.sandbox_approval` | `boolean` | 当为 `true` 时，允许沙盒升级批准提示浮现。 | | `approval_policy.granular.skill_approval` | `boolean` | 当为 `true` 时，允许技能脚本批准提示浮现。 | | `approvals_reviewer` | `user | auto_review` | 在 `on-request` 或粒度批准政策下审查合格批准提示的人。默认值为 `user`；`auto_review` 使用审核子代理。此设置不会更改在沙盒内已允许的沙盒和审核操作。 | | `apps._default.destructive_enabled` | `boolean` | 具有 `destructive_hint = true` 的应用工具的默认允许/拒绝。 | | `apps._default.enabled` | `boolean` | 所有应用的默认启用状态，除非每个应用覆盖。 | | `apps._default.open_world_enabled` | `boolean` | 具有 `open_world_hint = true` 的应用工具的默认允许/拒绝。 | | `apps.<id>.default_tools_approval_mode` | `auto | prompt | approve` | 此应用内工具的默认批准行为，除非存在逐工具覆盖。 | | `apps.<id>.default_tools_enabled` | `boolean` | 此应用内工具的默认启用状态，除非存在逐工具覆盖。 | | `apps.<id>.destructive_enabled` | `boolean` | 允许或阻止在此应用中宣传 `destructive_hint = true` 的工具。 | | `apps.<id>.enabled` | `boolean` | 根据 id 启用或禁用特定应用/连接器（默认：true）。 | | `apps.<id>.open_world_enabled` | `boolean` | 允许或阻止在此应用中宣传 `open_world_hint = true` 的工具。 | | `apps.<id>.tools.<tool>.approval_mode` | `auto | prompt | approve` | 单个应用工具的逐工具批准行为覆盖。 | | `apps.<id>.tools.<tool>.enabled` | `boolean` | 应用工具的逐工具启用覆盖（例如 `repos/list`）。 | | `auto_review.policy` | `string` | 用于自动审核的本地 Markdown 政策指令。托管的 `guardian_policy_config` 优先。空白值将被忽略。 | | `background_terminal_max_timeout` | `number` | 最大轮询窗口（以毫秒为单位），用于空 `write_stdin` 轮询（后端终端轮询）。默认值：`300000`（5分钟）。替代旧的 `background_terminal_timeout` 键。 | | `chatgpt_base_url` | `string` | 用于 ChatGPT 登录流程的基础 URL 覆盖。 | | `check_for_update_on_startup` | `boolean` | 启动时检查 Codex 更新（仅在更新集中管理时设置为 false）。 | | `cli_auth_credentials_store` | `file | keyring | auto` | 控制 CLI 存储的缓存凭据的位置（基于文件的 auth.json 与操作系统钥匙串）。 | | `commit_attribution` | `string` | 覆盖提交共同作者的附加文本。设置为空字符串以禁用自动归属。 | | `compact_prompt` | `string` | 历史压缩提示的行内覆盖。 | | `default_permissions` | `string` | 要应用于沙盒工具调用的默认权限配置文件的名称。 | | `developer_instructions` | `string` | 注入到会话中的额外开发者指令（可选）。 | | `disable_paste_burst` | `boolean` | 禁用 TUI 中的突发粘贴检测。 | | `experimental_compact_prompt_file` | `string (path)` | 从文件加载压缩提示覆盖（实验性）。 | | `experimental_use_unified_exec_tool` | `boolean` | 启用统一执行的旧名称；更倾向于使用 `[features].unified_exec` 或 `codex --enable unified_exec`。 | | `features.apps` | `boolean` | 启用 ChatGPT 应用/连接器支持（实验性）。 | | `features.codex_hooks` | `boolean` | 启用从 `hooks.json` 或行内 `[hooks]` 配置加载的生命周期钩子。 | | `features.enable_request_compression` | `boolean` | 当支持时，使用 zstd 压缩流请求体（稳定；默认开启）。 | | `features.fast_mode` | `boolean` | 启用快速模式选择和 `service_tier = "fast"` 路径（稳定；默认开启）。 | | `features.memories` | `boolean` | 启用 记忆（默认关闭）。 | | `features.multi_agent` | `boolean` | 启用多代理协作工具（`spawn_agent`、`send_input`、`resume_agent`、`wait_agent` 和 `close_agent`）（稳定；默认开启）。 | | `features.personality` | `boolean` | 启用个性选择控制（稳定；默认开启）。 | | `features.prevent_idle_sleep` | `boolean` | 防止机器在积极执行转弯时进入睡眠状态（实验性；默认关闭）。 | | `features.shell_snapshot` | `boolean` | 快照 shell 环境，以加速重复命令的执行（稳定；默认开启）。 | | `features.shell_tool` | `boolean` | 启用用于运行命令的默认 `shell` 工具（稳定；默认开启）。 | | `features.skill_mcp_dependency_install` | `boolean` | 允许提示和安装技能缺失的 MCP 依赖项（稳定；默认开启）。 | | `features.undo` | `boolean` | 启用撤销支持（稳定；默认关闭）。 | | `features.unified_exec` | `boolean` | 使用统一的 PTY 支持的执行工具（稳定；默认为默认开启，Windows 除外）。 | | `features.web_search` | `boolean` | 已弃用的旧切换；更倾向于使用顶级的 `web_search` 设置。 | | `features.web_search_cached` | `boolean` | 已弃用的旧切换。当 `web_search` 未设置时，`true` 映射到 `web_search = "cached"`。 | | `features.web_search_request` | `boolean` | 已弃用的旧切换。当 `web_search` 未设置时，`true` 映射到 `web_search = "live"`。 | | `feedback.enabled` | `boolean` | 启用通过 `/feedback` 提交反馈的功能，跨 Codex 表面（默认：true）。 | | `file_opener` | `vscode | vscode-insiders | windsurf | cursor | none` | 用于从 Codex 输出打开引文的 URI 方案（默认：`vscode`）。 | | `forced_chatgpt_workspace_id` | `string (uuid)` | 将 ChatGPT 登录限制在特定工作区标识符。 | | `forced_login_method` | `chatgpt | api` | 将 Codex 限制在特定身份验证方法中。 | | `hide_agent_reasoning` | `boolean` | 在 TUI 和 `codex exec` 输出中抑制推理事件。 | | `history.max_bytes` | `number` | 如果设置，限制历史文件大小（以字节为单位），通过删除最旧条目来限制。 | | `history.persistence` | `save-all | none` | 控制 Codex 是否将会话记录保存到 history.jsonl。 | | `hooks` | `table` | 在 `config.toml` 中配置的生命周期钩子。使用与 `hooks.json` 相同的事件模式；有关示例和支持的事件，请参阅钩子指南。 | | `instructions` | `string` | 保留以供将来使用；更倾向于使用 `model_instructions_file` 或 `AGENTS.md`。 | | `log_dir` | `string (path)` | Codex 写入日志文件的目录（例如 `codex-tui.log`）；默认为 `$CODEX_HOME/log`。 | | `mcp_oauth_callback_port` | `integer` | 用于在 MCP OAuth 登录期间使用的本地 HTTP 回叫服务器的可选固定端口。当未设置时，Codex 绑定到操作系统选择的临时端口。 | | `mcp_oauth_callback_url` | `string` | MCP OAuth 登录的可选重定向 URI 覆盖（例如开发箱入口 URL）。`mcp_oauth_callback_port` 仍然控制回调侦听器端口。 | | `mcp_oauth_credentials_store` | `auto | file | keyring` | 首选的 MCP OAuth 凭据存储位置。 | | `mcp_servers.<id>.args` | `array<string>` | 传递给 MCP 标准输入输出服务器命令的参数。 | | `mcp_servers.<id>.bearer_token_env_var` | `string` | 提供 MCP HTTP 服务器的 Bearer 令牌的环境变量。 | | `mcp_servers.<id>.command` | `string` | 启动的 MCP 标准输入输出服务器的命令。 | | `mcp_servers.<id>.cwd` | `string` | MCP 标准输入输出服务器进程的工作目录。 | | `mcp_servers.<id>.disabled_tools` | `array<string>` | 在 MCP 服务器启用后应用的拒绝名单。 | | `mcp_servers.<id>.enabled` | `boolean` | 在不删除其配置的情况下禁用 MCP 服务器。 | | `mcp_servers.<id>.enabled_tools` | `array<string>` | MCP 服务器暴露的工具名称的允许列表。 | | `mcp_servers.<id>.env` | `map<string,string>` | 转发到 MCP 标准输入输出服务器的环境变量。 | | `mcp_servers.<id>.env_http_headers` | `map<string,string>` | 从环境变量填充的 MCP HTTP 服务器的 HTTP 头。 | | `mcp_servers.<id>.env_vars` | `array<string | { name = string, source = "local" | "remote" }>` | 允许白名单中额外的环境变量，用于 MCP 标准输入输出服务器。字符串条目默认使用 `source = "local"`；仅在执行者支持的远程标准输入输出中使用 `source = "remote"`。 | | `mcp_servers.<id>.experimental_environment` | `local | remote` | MCP 服务器的实验性放置。`remote` 通过远程执行环境启动标准输入输出服务器；可流式 HTTP 远程放置尚未实现。 | | `mcp_servers.<id>.http_headers` | `map<string,string>` | 每个 MCP HTTP 请求中包含的静态 HTTP 头。 | | `mcp_servers.<id>.oauth_resource` | `string` | 在 MCP 登录期间包含的可选 RFC 8707 OAuth 资源参数。 | | `mcp_servers.<id>.required` | `boolean` | 当为 true 时，如果此启用的 MCP 服务器无法初始化，则启动/恢复失败。 | | `mcp_servers.<id>.scopes` | `array<string>` | 在对此 MCP 服务器进行身份验证时请求的 OAuth 范围。 | | `mcp_servers.<id>.startup_timeout_ms` | `number` | `startup_timeout_sec` 的别名，以毫秒为单位。 | | `mcp_servers.<id>.startup_timeout_sec` | `number` | 覆盖 MCP 服务器的默认 10s 启动超时。 | | `mcp_servers.<id>.tool_timeout_sec` | `number` | 覆盖 MCP 服务器的默认每工具 60s 超时。 | | `mcp_servers.<id>.url` | `string` | MCP 可流式 HTTP 服务器的端点。 | | `memories.consolidation_model` | `string` | 全球记忆整合的可选模型覆盖。 | | `memories.disable_on_external_context` | `boolean` | 当为 `true` 时，使用外部上下文（例如 MCP 工具调用、网络搜索或工具搜索）的线程将不会被纳入记忆生成。默认值为 `false`。旧别名：`memories.no_memories_if_mcp_or_web_search`。 | | `memories.extract_model` | `string` | 每线程记忆提取的可选模型覆盖。 | | `memories.generate_memories` | `boolean` | 当为 `false` 时，新创建的线程不会作为记忆生成输入存储。默认值为 `true`。 | | `memories.max_raw_memories_for_consolidation` | `number` | 全球整合中保留的最近原始记忆的最大数量。默认值为 `256`，上限为 `4096`。 | | `memories.max_rollout_age_days` | `number` | 进行记忆生成的线程的最大年龄。默认值为 `30`，限制为 `0`-`90`。 | | `memories.max_rollouts_per_startup` | `number` | 每次启动传递处理的最大 rollout 候选。默认值为 `16`，上限为 `128`。 | | `memories.max_unused_days` | `number` | 在记忆上次使用后变得不合适以进行整合的最大天数。默认值为 `30`，限制为 `0`-`365`。 | | `memories.min_rollout_idle_hours` | `number` | 将线程视为进行记忆生成的最小空闲时间。默认值为 `6`，限制为 `1`-`48`。 | | `memories.use_memories` | `boolean` | 当为 `false` 时，Codex 跳过将现有记忆注入到未来会话。默认值为 `true`。 | | `model` | `string` | 要使用的模型（例如 `gpt-5.5`）。 | | `model_auto_compact_token_limit` | `number` | 触发自动历史压缩的令牌阈值（未设置使用模型默认）。 | | `model_catalog_json` | `string (path)` | 在启动时加载的 JSON 模型目录的可选路径。配置文件级的 `profiles.<name>.model_catalog_json` 可以覆盖此值。 | | `model_context_window` | `number` | 可用于活动模型的上下文窗口令牌。 | | `model_instructions_file` | `string (path)` | 用于替代内置指令，而不是 `AGENTS.md`。 | | `model_provider` | `string` | 来自 `model_providers` 的提供程序 ID（默认值：`openai`）。 | | `model_providers.<id>` | `table` | 自定义提供程序定义。内置提供程序 ID（`openai`、`ollama` 和 `lmstudio`）是保留的，无法覆盖。 | | `model_providers.<id>.auth` | `table` | 自定义提供程序的命令支持 Bearer 令牌配置。请勿与 `env_key`、`experimental_bearer_token` 或 `requires_openai_auth` 一起使用。 | | `model_providers.<id>.auth.args` | `array<string>` | 传递给令牌命令的参数。 | | `model_providers.<id>.auth.command` | `string` | Codex 需要 Bearer 令牌时要运行的命令。该命令必须将令牌打印到 stdout。 | | `model_providers.<id>.auth.cwd` | `string (path)` | 令牌命令的工作目录。 | | `model_providers.<id>.auth.refresh_interval_ms` | `number` | Codex 主动刷新令牌的频率（默认：300000）。设置为 `0` 仅在身份验证重试后刷新。 | | `model_providers.<id>.auth.timeout_ms` | `number` | 令牌命令的最大运行时间（默认：5000）。 | | `model_providers.<id>.base_url` | `string` | 模型提供程序的 API 基础 URL。 | | `model_providers.<id>.env_http_headers` | `map<string,string>` | 存在时，从环境变量填充 HTTP 头。 | | `model_providers.<id>.env_key` | `string` | 提供程序 API 密钥的环境变量。 | | `model_providers.<id>.env_key_instructions` | `string` | 提供程序 API 密钥的可选设置指导。 | | `model_providers.<id>.experimental_bearer_token` | `string` | 提供程序的直接 Bearer 令牌（不建议使用；使用 `env_key`）。 | | `model_providers.<id>.http_headers` | `map<string,string>` | 添加到提供程序请求的静态 HTTP 头。 | | `model_providers.<id>.name` | `string` | 自定义模型提供程序的显示名称。 | | `model_providers.<id>.query_params` | `map<string,string>` | 附加到提供程序请求的额外查询参数。 | | `model_providers.<id>.request_max_retries` | `number` | 对提供程序的 HTTP 请求的重试次数（默认：4）。 | | `model_providers.<id>.requires_openai_auth` | `boolean` | 提供程序使用 OpenAI 身份验证（默认值为 false）。 | | `model_providers.<id>.stream_idle_timeout_ms` | `number` | SSE 流的空闲超时（以毫秒为单位，默认值：300000）。 | | `model_providers.<id>.stream_max_retries` | `number` | SSE 流中断的重试次数（默认：5）。 | | `model_providers.<id>.supports_websockets` | `boolean` | 该提供程序是否支持 Responses API WebSocket 传输。 | | `model_providers.<id>.wire_api` | `responses` | 提供程序使用的协议。当省略时，`responses` 是唯一支持的值，也是默认值。 | | `model_reasoning_effort` | `minimal | low | medium | high | xhigh` | 调整支持模型的推理努力（仅限 Responses API；`xhigh` 依赖于模型）。 | | `model_reasoning_summary` | `auto | concise | detailed | none` | 选择推理总结的详细程度或完全禁用总结。 | | `model_supports_reasoning_summaries` | `boolean` | 强制 Codex 发送或不发送推理元数据。 | | `model_verbosity` | `low | medium | high` | 可选的 GPT-5 Responses API 详细级别覆盖；当未设置时，使用所选模型/预设的默认值。 | | `notice.hide_full_access_warning` | `boolean` | 跟踪对完全访问警告提示的确认。 | | `notice.hide_gpt-5.1-codex-max_migration_prompt` | `boolean` | 跟踪对 gpt-5.1-codex-max 迁移提示的确认。 | | `notice.hide_gpt5_1_migration_prompt` | `boolean` | 跟踪对 GPT-5.1 迁移提示的确认。 | | `notice.hide_rate_limit_model_nudge` | `boolean` | 跟踪对速率限制模型切换提醒的选择退出。 | | `notice.hide_world_writable_warning` | `boolean` | 跟踪对 Windows 世界可写目录警告的确认。 | | `notice.model_migrations` | `map<string,string>` | 跟踪确认的模型迁移，作为旧->新映射。 | | `notify` | `array<string>` | 用于通知的命令；接收来自 Codex 的 JSON 负载。 | | `openai_base_url` | `string` | 内置 `openai` 模型提供程序的基础 URL 覆盖。 | | `oss_provider` | `lmstudio | ollama` | 在运行时默认使用的本地提供程序，使用 `--oss` 时（如果未设置则默认提 prompt）。 | | `otel.environment` | `string` | 应用于发出的 OpenTelemetry 事件的环境标签（默认：`dev`）。 | | `otel.exporter` | `none | otlp-http | otlp-grpc` | 选择 OpenTelemetry 导出器并提供任何端点元数据。 | | `otel.exporter.<id>.endpoint` | `string` | OTEL 日志的导出器端点。 | | `otel.exporter.<id>.headers` | `map<string,string>` | 包含在 OTEL 导出器请求中的静态头。 | | `otel.exporter.<id>.protocol` | `binary | json` | OTLP/HTTP 导出器使用的协议。 | | `otel.exporter.<id>.tls.ca-certificate` | `string` | OTEL 导出器 TLS 的 CA 证书路径。 | | `otel.exporter.<id>.tls.client-certificate` | `string` | OTEL 导出器 TLS 的客户端证书路径。 | | `otel.exporter.<id>.tls.client-private-key` | `string` | OTEL 导出器 TLS 的客户端私钥路径。 | | `otel.log_user_prompt` | `boolean` | 选择导出原始用户提示与 OpenTelemetry 日志。 | | `otel.metrics_exporter` | `none | statsig | otlp-http | otlp-grpc` | 选择 OpenTelemetry 指标导出器（默认为 `statsig`）。 | | `otel.trace_exporter` | `none | otlp-http | otlp-grpc` | 选择 OpenTelemetry 跟踪导出器并提供任何端点元数据。 | | `otel.trace_exporter.<id>.endpoint` | `string` | OTEL 日志的追踪导出器端点。 | | `otel.trace_exporter.<id>.headers` | `map<string,string>` | 包含在 OTEL 跟踪导出器请求中的静态头。 | | `otel.trace_exporter.<id>.protocol` | `binary | json` | OTLP/HTTP 跟踪导出器使用的协议。 | | `otel.trace_exporter.<id>.tls.ca-certificate` | `string` | OTEL 跟踪导出器 TLS 的 CA 证书路径。 | | `otel.trace_exporter.<id>.tls.client-certificate` | `string` | OTEL 跟踪导出器 TLS 的客户端证书路径。 | | `otel.trace_exporter.<id>.tls.client-private-key` | `string` | OTEL 跟踪导出器 TLS 的客户端私钥路径。 | | `permissions.<name>.filesystem` | `table` | 命名文件系统权限配置文件。每个键是绝对路径或特殊令牌，例如 `:minimal` 或 `:project_roots`。 | | `permissions.<name>.filesystem.":project_roots".<subpath-or-glob>` | `"read" | "write" | "none"` | 相对于检测到的项目根目录的文件系统访问范围。使用 `"."` 表示根本身；可以通过 `"**/*.env"` 这样的全局子路径拒绝读取，并使用 `"none"`。 | | `permissions.<name>.filesystem.<path-or-glob>` | `"read" | "write" | "none" | table` | 授予路径、全局模式或特殊令牌的直接访问，或根据该根范围内的嵌套条目。使用 `"none"` 拒绝匹配路径的读取。 | | `permissions.<name>.filesystem.glob_scan_max_depth` | `number` | 在平台上为展开拒绝读取全局模式设置的最大深度，必须在设置时至少为 `1`。 | | `permissions.<name>.network.allow_local_binding` | `boolean` | 允许通过管理代理进行本地绑定/监听操作。 | | `permissions.<name>.network.allow_upstream_proxy` | `boolean` | 允许管理代理链接到另一个上游代理。 | | `permissions.<name>.network.dangerously_allow_all_unix_sockets` | `boolean` | 允许代理使用任意 Unix 套接字，而不是默认的限制集。 | | `permissions.<name>.network.dangerously_allow_non_loopback_proxy` | `boolean` | 允许为管理代理监听器提供非回环绑定地址。 | | `permissions.<name>.network.domains` | `map<string, allow | deny>` | 管理代理的域规则。使用域名或通配符模式作为键，具有 `allow` 或 `deny` 值。 | | `permissions.<name>.network.enable_socks5` | `boolean` | 当此权限配置文件启用管理网络代理时，开启 SOCKS5 监听。 | | `permissions.<name>.network.enable_socks5_udp` | `boolean` | 允许在启用时通过 SOCKS5 监听进行 UDP。 | | `permissions.<name>.network.enabled` | `boolean` | 启用此命名权限配置文件的网络访问。 | | `permissions.<name>.network.mode` | `limited | full` | 用于子进程流量的网络代理模式。 | | `permissions.<name>.network.proxy_url` | `string` | 当此权限配置文件启用管理网络代理时使用的 HTTP 代理端点。 | | `permissions.<name>.network.socks_url` | `string` | 此权限配置文件使用的 SOCKS5 代理端点。 | | `permissions.<name>.network.unix_sockets` | `map<string, allow | none>` | 管理代理的 Unix 套接字规则。使用套接字路径作为键，具有 `allow` 或 `none` 值。 | | `personality` | `none | friendly | pragmatic` | 默认交流风格，适用于声明支持 `supportsPersonality` 的模型；可以在每个线程/轮次或通过 `/personality` 进行覆盖。 | | `plan_mode_reasoning_effort` | `none | minimal | low | medium | high | xhigh` | 特定于计划模式的推理覆盖。当未设置时，计划模式使用其内置的预设默认。 | | `profile` | `string` | 启动时应用的默认配置文件（等同于 `--profile`）。 | | `profiles.<name>.*` | `various` | 对任何受支持的配置键的配置文件范围的覆盖。 | | `profiles.<name>.analytics.enabled` | `boolean` | 配置文件范围的分析启用覆盖。 | | `profiles.<name>.experimental_use_unified_exec_tool` | `boolean` | 启用统一执行的旧名称；更倾向于使用 `[features].unified_exec`。 | | `profiles.<name>.model_catalog_json` | `string (path)` | 配置文件范围的模型目录 JSON 路径覆盖（仅在启动时应用；覆盖该配置文件的顶部 `model_catalog_json`）。 | | `profiles.<name>.model_instructions_file` | `string (path)` | 配置文件范围的内置指令文件替换。 | | `profiles.<name>.oss_provider` | `lmstudio | ollama` | Configured profile’s OSS provider per `--oss` 会话。 | | `profiles.<name>.personality` | `none | friendly | pragmatic` | 支持的模型的配置文件范围的交流风格覆盖。 | | `profiles.<name>.plan_mode_reasoning_effort` | `none | minimal | low | medium | high | xhigh` | 配置文件范围的计划模式推理覆盖。 | | `profiles.<name>.service_tier` | `flex | fast` | 配置文件范围的新转弯的服务等级偏好。 | | `profiles.<name>.tools_view_image` | `boolean` | 启用或禁用该配置文件中的 `view_image` 工具。 | | `profiles.<name>.web_search` | `disabled | cached | live` | 配置文件范围的网络搜索模式覆盖（默认：`"cached"`）。 | | `profiles.<name>.windows.sandbox` | `unelevated | elevated` | 配置文件范围的 Windows 沙盒模式覆盖。 | | `project_doc_fallback_filenames` | `array<string>` | 当 `AGENTS.md` 缺失时尝试的附加文件名。 | | `project_doc_max_bytes` | `number` | 在构建项目指令时从 `AGENTS.md` 读取的最大字节数。 | | `project_root_markers` | `array<string>` | 项目根标记文件名列表；用于在搜索父目录时确定项目根。 | | `projects.<path>.trust_level` | `string` | 将项目或工作树标记为受信任或不受信任（`"trusted"` \| `"untrusted"`）。不受信任的项目跳过项目范围的 `.codex/` 层，包括项目本地配置、钩子和规则。 | | `review_model` | `string` | `/review` 使用的可选模型覆盖（默认为当前会话模型）。 | | `sandbox_mode` | `read-only | workspace-write | danger-full-access` | 在命令执行期间的文件系统和网络访问沙盒策略。 | | `sandbox_workspace_write.exclude_slash_tmp` | `boolean` | 在工作区写入模式中从可写根目录中排除 `/tmp`。 | | `sandbox_workspace_write.exclude_tmpdir_env_var` | `boolean` | 在工作区写入模式中从可写根目录中排除 `$TMPDIR`。 | | `sandbox_workspace_write.network_access` | `boolean` | 允许在工作区写入沙盒内的出站网络访问。 | | `sandbox_workspace_write.writable_roots` | `array<string>` | 当 `sandbox_mode = "workspace-write"` 时的附加可写根。 | | `service_tier` | `flex | fast` | 新转弯的首选服务分层。 | | `shell_environment_policy.exclude` | `array<string>` | 用于在默认值之后删除环境变量的全局模式。 | | `shell_environment_policy.experimental_use_profile` | `boolean` | 在生成子进程时使用用户 shell 配置文件。 | | `shell_environment_policy.ignore_default_excludes` | `boolean` | 在其他过滤器运行之前保留包含 KEY/SECRET/TOKEN 的变量。 | | `shell_environment_policy.include_only` | `array<string>` | 模式的白名单；当设置时，仅保留匹配的变量。 | | `shell_environment_policy.inherit` | `all | core | none` | 生成子进程时的基线环境继承。 | | `shell_environment_policy.set` | `map<string,string>` | 注入到每个子进程的显式环境覆盖。 | | `show_raw_agent_reasoning` | `boolean` | 在当前模型发出原始推理内容时显示它。 | | `skills.config` | `array<object>` | 存储在 config.toml 中的每个技能的启用覆盖。 | | `skills.config.<index>.enabled` | `boolean` | 启用或禁用所引用的技能。 | | `skills.config.<index>.path` | `string (path)` | 包含 `SKILL.md` 的技能文件夹的路径。 | | `sqlite_home` | `string (path)` | Codex 存储用于代理作业和其它可恢复运行时状态的 SQLite 支持状态数据库的目录。 | | `suppress_unstable_features_warning` | `boolean` | 抑制在启用开发中的功能标志时出现的警告。 | | `tool_output_token_limit` | `number` | 用于在历史中存储单个工具/函数输出的令牌预算。 | | `tool_suggest.discoverables` | `array<table>` | 允许对更多可发现连接器或插件的工具建议。每个条目使用 `type = "connector"` 或 `"plugin"` 和一个 `id`。 | | `tools.view_image` | `boolean` | 启用本地图像附件工具 `view_image`。 | | `tools.web_search` | `boolean | { context_size = "low|medium|high", allowed_domains = [string], location = { country, region, city, timezone } }` | 可选的网络搜索工具配置。旧的布尔形式仍然被接受，但对象形式允许您设置搜索上下文大小、允许的域和近似用户位置。 | | `tui` | `table` | TUI 特定选项，例如启用内联桌面通知。 | | `tui.alternate_screen` | `auto | always | never` | 控制 TUI 的备用屏幕使用情况（默认：自动；自动在 Zellij 中跳过以保留向前滚动）。 | | `tui.animations` | `boolean` | 启用终端动画（欢迎屏幕、闪烁、旋转器）（默认：true）。 | | `tui.model_availability_nux.<model>` | `integer` | 由模型标记键控的内部启动工具提示状态。 | | `tui.notification_condition` | `unfocused | always` | 控制 TUI 通知是否仅在终端未聚焦时触发或无论聚焦状态如何。默认值为 `unfocused`。 | | `tui.notification_method` | `auto | osc9 | bel` | 终端通知的通知方法（默认：自动）。 | | `tui.notifications` | `boolean | array<string>` | 启用 TUI 通知；可选限制为特定事件类型。 | | `tui.show_tooltips` | `boolean` | 显示 TUI 欢迎屏幕中的入门工具提示（默认：true）。 | | `tui.status_line` | `array<string> | null` | TUI 页脚状态行项标识符的有序列表。`null` 禁用状态行。 | | `tui.terminal_title` | `array<string> | null` | 终端窗口/标签标题项标识符的有序列表。默认值为 `["spinner", "project"]`； `null` 禁用标题更新。 | | `tui.theme` | `string` | 语法高亮主题覆盖（短横线风格主题名称）。 | | `web_search` | `disabled | cached | live` | 网络搜索模式（默认：`"cached"`；cached 使用 OpenAI 维护的索引并不获取实时页面；如果您使用 `--yolo` 或其他完全访问沙盒设置，则它默认为 `"live"`）。使用 `"live"` 获取最新的数据，使用 `"disabled"` 移除该工具。 | | `windows_wsl_setup_acknowledged` | `boolean` | 跟踪 Windows 入门确认（仅适用于 Windows）。 | | `windows.sandbox` | `unelevated | elevated` | 在 Windows 上本地运行 Codex 时的 Windows 唯一原生沙盒模式。 | | `windows.sandbox_private_desktop` | `boolean` | 在本地 Windows 上默认在私人桌面上运行最终的沙盒子进程。仅在与较旧的 `Winsta0\\Default` 行为兼容时设置为 `false`。 |
键

把它理解成一个更严谨的过程：
1. 用户级配置保存在 `~/.codex/config.toml` 中。您还可以在 `.codex/config.toml` 文件中添加项目范围的覆盖。Codex 仅在您信任该项目时加载项目范围的配置文件。
2. 对于沙盒和批准密钥 (`approval_policy`、`sandbox_mode` 和 `sandbox_workspace_write.*`)，请将此参考与 沙盒和批准、可写根中的受保护路径 和 网络访问 一起使用。
3. | 键 | 类型 / 值 | 详情 | | --- | --- | --- | | `agents.<name>.config_file` | `string (path)` | 此角色的 TOML 配置层的路径；相对路径以声明角色的配置文件为基准解析。 | | `agents.<name>.description` | `string` | 在选择和产生该代理类型时，显示给 Codex 的角色指导。 | | `agents.<name>.nickname_candidates` | `array<string>` | 可选的显示昵称池，用于以该角色产生的代理。 | | `agents.job_max_runtime_seconds` | `number` | `spawn_agents_on_csv` 作业的每个工作者的默认超时。当未设置时，工具会回退到每个工作者 1800 秒。 | | `agents.max_depth` | `number` | 允许为产生的代理线程最大嵌套深度（根会话的深度为 0；默认值：1）。 | | `agents.max_threads` | `number` | 可以同时打开的代理线程的最大数量。当未设置时，默认为 `6`。 | | `allow_login_shell` | `boolean` | 允许基于 shell 的工具使用登录 shell 语义。默认值为 `true`；当为 `false` 时，`login = true` 请求被拒绝，而省略 `login` 默认使用非登录 shell。 | | `analytics.enabled` | `boolean` | 启用或禁用此机器/配置文件的分析功能。当未设置时，客户端默认应用。 | | `approval_policy` | `untrusted | on-request | never | { granular = { sandbox_approval = bool, rules = bool, mcp_elicitations = bool, request_permissions = bool, skill_approval = bool } }` | 控制 Codex 在执行命令之前何时暂停以进行批准。您还可以使用 `approval_policy = { granular = { ... } }` 允许或自动拒绝特定提示类别，同时保持其他提示的互动性。`on-failure` 已被弃用；对交互式运行使用 `on-request`，或对非交互式运行使用 `never`。 | | `approval_policy.granular.mcp_elicitations` | `boolean` | 当为 `true` 时，允许 MCP 引导提示浮现，而不是被自动拒绝。 | | `approval_policy.granular.request_permissions` | `boolean` | 当为 `true` 时，允许来自 `request_permissions` 工具的提示浮现。 | | `approval_policy.granular.rules` | `boolean` | 当为 `true` 时，允许由 execpolicy 的 `prompt` 规则触发的批准浮现。 | | `approval_policy.granular.sandbox_approval` | `boolean` | 当为 `true` 时，允许沙盒升级批准提示浮现。 | | `approval_policy.granular.skill_approval` | `boolean` | 当为 `true` 时，允许技能脚本批准提示浮现。 | | `approvals_reviewer` | `user | auto_review` | 在 `on-request` 或粒度批准政策下审查合格批准提示的人。默认值为 `user`；`auto_review` 使用审核子代理。此设置不会更改在沙盒内已允许的沙盒和审核操作。 | | `apps._default.destructive_enabled` | `boolean` | 具有 `destructive_hint = true` 的应用工具的默认允许/拒绝。 | | `apps._default.enabled` | `boolean` | 所有应用的默认启用状态，除非每个应用覆盖。 | | `apps._default.open_world_enabled` | `boolean` | 具有 `open_world_hint = true` 的应用工具的默认允许/拒绝。 | | `apps.<id>.default_tools_approval_mode` | `auto | prompt | approve` | 此应用内工具的默认批准行为，除非存在逐工具覆盖。 | | `apps.<id>.default_tools_enabled` | `boolean` | 此应用内工具的默认启用状态，除非存在逐工具覆盖。 | | `apps.<id>.destructive_enabled` | `boolean` | 允许或阻止在此应用中宣传 `destructive_hint = true` 的工具。 | | `apps.<id>.enabled` | `boolean` | 根据 id 启用或禁用特定应用/连接器（默认：true）。 | | `apps.<id>.open_world_enabled` | `boolean` | 允许或阻止在此应用中宣传 `open_world_hint = true` 的工具。 | | `apps.<id>.tools.<tool>.approval_mode` | `auto | prompt | approve` | 单个应用工具的逐工具批准行为覆盖。 | | `apps.<id>.tools.<tool>.enabled` | `boolean` | 应用工具的逐工具启用覆盖（例如 `repos/list`）。 | | `auto_review.policy` | `string` | 用于自动审核的本地 Markdown 政策指令。托管的 `guardian_policy_config` 优先。空白值将被忽略。 | | `background_terminal_max_timeout` | `number` | 最大轮询窗口（以毫秒为单位），用于空 `write_stdin` 轮询（后端终端轮询）。默认值：`300000`（5分钟）。替代旧的 `background_terminal_timeout` 键。 | | `chatgpt_base_url` | `string` | 用于 ChatGPT 登录流程的基础 URL 覆盖。 | | `check_for_update_on_startup` | `boolean` | 启动时检查 Codex 更新（仅在更新集中管理时设置为 false）。 | | `cli_auth_credentials_store` | `file | keyring | auto` | 控制 CLI 存储的缓存凭据的位置（基于文件的 auth.json 与操作系统钥匙串）。 | | `commit_attribution` | `string` | 覆盖提交共同作者的附加文本。设置为空字符串以禁用自动归属。 | | `compact_prompt` | `string` | 历史压缩提示的行内覆盖。 | | `default_permissions` | `string` | 要应用于沙盒工具调用的默认权限配置文件的名称。 | | `developer_instructions` | `string` | 注入到会话中的额外开发者指令（可选）。 | | `disable_paste_burst` | `boolean` | 禁用 TUI 中的突发粘贴检测。 | | `experimental_compact_prompt_file` | `string (path)` | 从文件加载压缩提示覆盖（实验性）。 | | `experimental_use_unified_exec_tool` | `boolean` | 启用统一执行的旧名称；更倾向于使用 `[features].unified_exec` 或 `codex --enable unified_exec`。 | | `features.apps` | `boolean` | 启用 ChatGPT 应用/连接器支持（实验性）。 | | `features.codex_hooks` | `boolean` | 启用从 `hooks.json` 或行内 `[hooks]` 配置加载的生命周期钩子。 | | `features.enable_request_compression` | `boolean` | 当支持时，使用 zstd 压缩流请求体（稳定；默认开启）。 | | `features.fast_mode` | `boolean` | 启用快速模式选择和 `service_tier = "fast"` 路径（稳定；默认开启）。 | | `features.memories` | `boolean` | 启用 记忆（默认关闭）。 | | `features.multi_agent` | `boolean` | 启用多代理协作工具（`spawn_agent`、`send_input`、`resume_agent`、`wait_agent` 和 `close_agent`）（稳定；默认开启）。 | | `features.personality` | `boolean` | 启用个性选择控制（稳定；默认开启）。 | | `features.prevent_idle_sleep` | `boolean` | 防止机器在积极执行转弯时进入睡眠状态（实验性；默认关闭）。 | | `features.shell_snapshot` | `boolean` | 快照 shell 环境，以加速重复命令的执行（稳定；默认开启）。 | | `features.shell_tool` | `boolean` | 启用用于运行命令的默认 `shell` 工具（稳定；默认开启）。 | | `features.skill_mcp_dependency_install` | `boolean` | 允许提示和安装技能缺失的 MCP 依赖项（稳定；默认开启）。 | | `features.undo` | `boolean` | 启用撤销支持（稳定；默认关闭）。 | | `features.unified_exec` | `boolean` | 使用统一的 PTY 支持的执行工具（稳定；默认为默认开启，Windows 除外）。 | | `features.web_search` | `boolean` | 已弃用的旧切换；更倾向于使用顶级的 `web_search` 设置。 | | `features.web_search_cached` | `boolean` | 已弃用的旧切换。当 `web_search` 未设置时，`true` 映射到 `web_search = "cached"`。 | | `features.web_search_request` | `boolean` | 已弃用的旧切换。当 `web_search` 未设置时，`true` 映射到 `web_search = "live"`。 | | `feedback.enabled` | `boolean` | 启用通过 `/feedback` 提交反馈的功能，跨 Codex 表面（默认：true）。 | | `file_opener` | `vscode | vscode-insiders | windsurf | cursor | none` | 用于从 Codex 输出打开引文的 URI 方案（默认：`vscode`）。 | | `forced_chatgpt_workspace_id` | `string (uuid)` | 将 ChatGPT 登录限制在特定工作区标识符。 | | `forced_login_method` | `chatgpt | api` | 将 Codex 限制在特定身份验证方法中。 | | `hide_agent_reasoning` | `boolean` | 在 TUI 和 `codex exec` 输出中抑制推理事件。 | | `history.max_bytes` | `number` | 如果设置，限制历史文件大小（以字节为单位），通过删除最旧条目来限制。 | | `history.persistence` | `save-all | none` | 控制 Codex 是否将会话记录保存到 history.jsonl。 | | `hooks` | `table` | 在 `config.toml` 中配置的生命周期钩子。使用与 `hooks.json` 相同的事件模式；有关示例和支持的事件，请参阅钩子指南。 | | `instructions` | `string` | 保留以供将来使用；更倾向于使用 `model_instructions_file` 或 `AGENTS.md`。 | | `log_dir` | `string (path)` | Codex 写入日志文件的目录（例如 `codex-tui.log`）；默认为 `$CODEX_HOME/log`。 | | `mcp_oauth_callback_port` | `integer` | 用于在 MCP OAuth 登录期间使用的本地 HTTP 回叫服务器的可选固定端口。当未设置时，Codex 绑定到操作系统选择的临时端口。 | | `mcp_oauth_callback_url` | `string` | MCP OAuth 登录的可选重定向 URI 覆盖（例如开发箱入口 URL）。`mcp_oauth_callback_port` 仍然控制回调侦听器端口。 | | `mcp_oauth_credentials_store` | `auto | file | keyring` | 首选的 MCP OAuth 凭据存储位置。 | | `mcp_servers.<id>.args` | `array<string>` | 传递给 MCP 标准输入输出服务器命令的参数。 | | `mcp_servers.<id>.bearer_token_env_var` | `string` | 提供 MCP HTTP 服务器的 Bearer 令牌的环境变量。 | | `mcp_servers.<id>.command` | `string` | 启动的 MCP 标准输入输出服务器的命令。 | | `mcp_servers.<id>.cwd` | `string` | MCP 标准输入输出服务器进程的工作目录。 | | `mcp_servers.<id>.disabled_tools` | `array<string>` | 在 MCP 服务器启用后应用的拒绝名单。 | | `mcp_servers.<id>.enabled` | `boolean` | 在不删除其配置的情况下禁用 MCP 服务器。 | | `mcp_servers.<id>.enabled_tools` | `array<string>` | MCP 服务器暴露的工具名称的允许列表。 | | `mcp_servers.<id>.env` | `map<string,string>` | 转发到 MCP 标准输入输出服务器的环境变量。 | | `mcp_servers.<id>.env_http_headers` | `map<string,string>` | 从环境变量填充的 MCP HTTP 服务器的 HTTP 头。 | | `mcp_servers.<id>.env_vars` | `array<string | { name = string, source = "local" | "remote" }>` | 允许白名单中额外的环境变量，用于 MCP 标准输入输出服务器。字符串条目默认使用 `source = "local"`；仅在执行者支持的远程标准输入输出中使用 `source = "remote"`。 | | `mcp_servers.<id>.experimental_environment` | `local | remote` | MCP 服务器的实验性放置。`remote` 通过远程执行环境启动标准输入输出服务器；可流式 HTTP 远程放置尚未实现。 | | `mcp_servers.<id>.http_headers` | `map<string,string>` | 每个 MCP HTTP 请求中包含的静态 HTTP 头。 | | `mcp_servers.<id>.oauth_resource` | `string` | 在 MCP 登录期间包含的可选 RFC 8707 OAuth 资源参数。 | | `mcp_servers.<id>.required` | `boolean` | 当为 true 时，如果此启用的 MCP 服务器无法初始化，则启动/恢复失败。 | | `mcp_servers.<id>.scopes` | `array<string>` | 在对此 MCP 服务器进行身份验证时请求的 OAuth 范围。 | | `mcp_servers.<id>.startup_timeout_ms` | `number` | `startup_timeout_sec` 的别名，以毫秒为单位。 | | `mcp_servers.<id>.startup_timeout_sec` | `number` | 覆盖 MCP 服务器的默认 10s 启动超时。 | | `mcp_servers.<id>.tool_timeout_sec` | `number` | 覆盖 MCP 服务器的默认每工具 60s 超时。 | | `mcp_servers.<id>.url` | `string` | MCP 可流式 HTTP 服务器的端点。 | | `memories.consolidation_model` | `string` | 全球记忆整合的可选模型覆盖。 | | `memories.disable_on_external_context` | `boolean` | 当为 `true` 时，使用外部上下文（例如 MCP 工具调用、网络搜索或工具搜索）的线程将不会被纳入记忆生成。默认值为 `false`。旧别名：`memories.no_memories_if_mcp_or_web_search`。 | | `memories.extract_model` | `string` | 每线程记忆提取的可选模型覆盖。 | | `memories.generate_memories` | `boolean` | 当为 `false` 时，新创建的线程不会作为记忆生成输入存储。默认值为 `true`。 | | `memories.max_raw_memories_for_consolidation` | `number` | 全球整合中保留的最近原始记忆的最大数量。默认值为 `256`，上限为 `4096`。 | | `memories.max_rollout_age_days` | `number` | 进行记忆生成的线程的最大年龄。默认值为 `30`，限制为 `0`-`90`。 | | `memories.max_rollouts_per_startup` | `number` | 每次启动传递处理的最大 rollout 候选。默认值为 `16`，上限为 `128`。 | | `memories.max_unused_days` | `number` | 在记忆上次使用后变得不合适以进行整合的最大天数。默认值为 `30`，限制为 `0`-`365`。 | | `memories.min_rollout_idle_hours` | `number` | 将线程视为进行记忆生成的最小空闲时间。默认值为 `6`，限制为 `1`-`48`。 | | `memories.use_memories` | `boolean` | 当为 `false` 时，Codex 跳过将现有记忆注入到未来会话。默认值为 `true`。 | | `model` | `string` | 要使用的模型（例如 `gpt-5.5`）。 | | `model_auto_compact_token_limit` | `number` | 触发自动历史压缩的令牌阈值（未设置使用模型默认）。 | | `model_catalog_json` | `string (path)` | 在启动时加载的 JSON 模型目录的可选路径。配置文件级的 `profiles.<name>.model_catalog_json` 可以覆盖此值。 | | `model_context_window` | `number` | 可用于活动模型的上下文窗口令牌。 | | `model_instructions_file` | `string (path)` | 用于替代内置指令，而不是 `AGENTS.md`。 | | `model_provider` | `string` | 来自 `model_providers` 的提供程序 ID（默认值：`openai`）。 | | `model_providers.<id>` | `table` | 自定义提供程序定义。内置提供程序 ID（`openai`、`ollama` 和 `lmstudio`）是保留的，无法覆盖。 | | `model_providers.<id>.auth` | `table` | 自定义提供程序的命令支持 Bearer 令牌配置。请勿与 `env_key`、`experimental_bearer_token` 或 `requires_openai_auth` 一起使用。 | | `model_providers.<id>.auth.args` | `array<string>` | 传递给令牌命令的参数。 | | `model_providers.<id>.auth.command` | `string` | Codex 需要 Bearer 令牌时要运行的命令。该命令必须将令牌打印到 stdout。 | | `model_providers.<id>.auth.cwd` | `string (path)` | 令牌命令的工作目录。 | | `model_providers.<id>.auth.refresh_interval_ms` | `number` | Codex 主动刷新令牌的频率（默认：300000）。设置为 `0` 仅在身份验证重试后刷新。 | | `model_providers.<id>.auth.timeout_ms` | `number` | 令牌命令的最大运行时间（默认：5000）。 | | `model_providers.<id>.base_url` | `string` | 模型提供程序的 API 基础 URL。 | | `model_providers.<id>.env_http_headers` | `map<string,string>` | 存在时，从环境变量填充 HTTP 头。 | | `model_providers.<id>.env_key` | `string` | 提供程序 API 密钥的环境变量。 | | `model_providers.<id>.env_key_instructions` | `string` | 提供程序 API 密钥的可选设置指导。 | | `model_providers.<id>.experimental_bearer_token` | `string` | 提供程序的直接 Bearer 令牌（不建议使用；使用 `env_key`）。 | | `model_providers.<id>.http_headers` | `map<string,string>` | 添加到提供程序请求的静态 HTTP 头。 | | `model_providers.<id>.name` | `string` | 自定义模型提供程序的显示名称。 | | `model_providers.<id>.query_params` | `map<string,string>` | 附加到提供程序请求的额外查询参数。 | | `model_providers.<id>.request_max_retries` | `number` | 对提供程序的 HTTP 请求的重试次数（默认：4）。 | | `model_providers.<id>.requires_openai_auth` | `boolean` | 提供程序使用 OpenAI 身份验证（默认值为 false）。 | | `model_providers.<id>.stream_idle_timeout_ms` | `number` | SSE 流的空闲超时（以毫秒为单位，默认值：300000）。 | | `model_providers.<id>.stream_max_retries` | `number` | SSE 流中断的重试次数（默认：5）。 | | `model_providers.<id>.supports_websockets` | `boolean` | 该提供程序是否支持 Responses API WebSocket 传输。 | | `model_providers.<id>.wire_api` | `responses` | 提供程序使用的协议。当省略时，`responses` 是唯一支持的值，也是默认值。 | | `model_reasoning_effort` | `minimal | low | medium | high | xhigh` | 调整支持模型的推理努力（仅限 Responses API；`xhigh` 依赖于模型）。 | | `model_reasoning_summary` | `auto | concise | detailed | none` | 选择推理总结的详细程度或完全禁用总结。 | | `model_supports_reasoning_summaries` | `boolean` | 强制 Codex 发送或不发送推理元数据。 | | `model_verbosity` | `low | medium | high` | 可选的 GPT-5 Responses API 详细级别覆盖；当未设置时，使用所选模型/预设的默认值。 | | `notice.hide_full_access_warning` | `boolean` | 跟踪对完全访问警告提示的确认。 | | `notice.hide_gpt-5.1-codex-max_migration_prompt` | `boolean` | 跟踪对 gpt-5.1-codex-max 迁移提示的确认。 | | `notice.hide_gpt5_1_migration_prompt` | `boolean` | 跟踪对 GPT-5.1 迁移提示的确认。 | | `notice.hide_rate_limit_model_nudge` | `boolean` | 跟踪对速率限制模型切换提醒的选择退出。 | | `notice.hide_world_writable_warning` | `boolean` | 跟踪对 Windows 世界可写目录警告的确认。 | | `notice.model_migrations` | `map<string,string>` | 跟踪确认的模型迁移，作为旧->新映射。 | | `notify` | `array<string>` | 用于通知的命令；接收来自 Codex 的 JSON 负载。 | | `openai_base_url` | `string` | 内置 `openai` 模型提供程序的基础 URL 覆盖。 | | `oss_provider` | `lmstudio | ollama` | 在运行时默认使用的本地提供程序，使用 `--oss` 时（如果未设置则默认提 prompt）。 | | `otel.environment` | `string` | 应用于发出的 OpenTelemetry 事件的环境标签（默认：`dev`）。 | | `otel.exporter` | `none | otlp-http | otlp-grpc` | 选择 OpenTelemetry 导出器并提供任何端点元数据。 | | `otel.exporter.<id>.endpoint` | `string` | OTEL 日志的导出器端点。 | | `otel.exporter.<id>.headers` | `map<string,string>` | 包含在 OTEL 导出器请求中的静态头。 | | `otel.exporter.<id>.protocol` | `binary | json` | OTLP/HTTP 导出器使用的协议。 | | `otel.exporter.<id>.tls.ca-certificate` | `string` | OTEL 导出器 TLS 的 CA 证书路径。 | | `otel.exporter.<id>.tls.client-certificate` | `string` | OTEL 导出器 TLS 的客户端证书路径。 | | `otel.exporter.<id>.tls.client-private-key` | `string` | OTEL 导出器 TLS 的客户端私钥路径。 | | `otel.log_user_prompt` | `boolean` | 选择导出原始用户提示与 OpenTelemetry 日志。 | | `otel.metrics_exporter` | `none | statsig | otlp-http | otlp-grpc` | 选择 OpenTelemetry 指标导出器（默认为 `statsig`）。 | | `otel.trace_exporter` | `none | otlp-http | otlp-grpc` | 选择 OpenTelemetry 跟踪导出器并提供任何端点元数据。 | | `otel.trace_exporter.<id>.endpoint` | `string` | OTEL 日志的追踪导出器端点。 | | `otel.trace_exporter.<id>.headers` | `map<string,string>` | 包含在 OTEL 跟踪导出器请求中的静态头。 | | `otel.trace_exporter.<id>.protocol` | `binary | json` | OTLP/HTTP 跟踪导出器使用的协议。 | | `otel.trace_exporter.<id>.tls.ca-certificate` | `string` | OTEL 跟踪导出器 TLS 的 CA 证书路径。 | | `otel.trace_exporter.<id>.tls.client-certificate` | `string` | OTEL 跟踪导出器 TLS 的客户端证书路径。 | | `otel.trace_exporter.<id>.tls.client-private-key` | `string` | OTEL 跟踪导出器 TLS 的客户端私钥路径。 | | `permissions.<name>.filesystem` | `table` | 命名文件系统权限配置文件。每个键是绝对路径或特殊令牌，例如 `:minimal` 或 `:project_roots`。 | | `permissions.<name>.filesystem.":project_roots".<subpath-or-glob>` | `"read" | "write" | "none"` | 相对于检测到的项目根目录的文件系统访问范围。使用 `"."` 表示根本身；可以通过 `"**/*.env"` 这样的全局子路径拒绝读取，并使用 `"none"`。 | | `permissions.<name>.filesystem.<path-or-glob>` | `"read" | "write" | "none" | table` | 授予路径、全局模式或特殊令牌的直接访问，或根据该根范围内的嵌套条目。使用 `"none"` 拒绝匹配路径的读取。 | | `permissions.<name>.filesystem.glob_scan_max_depth` | `number` | 在平台上为展开拒绝读取全局模式设置的最大深度，必须在设置时至少为 `1`。 | | `permissions.<name>.network.allow_local_binding` | `boolean` | 允许通过管理代理进行本地绑定/监听操作。 | | `permissions.<name>.network.allow_upstream_proxy` | `boolean` | 允许管理代理链接到另一个上游代理。 | | `permissions.<name>.network.dangerously_allow_all_unix_sockets` | `boolean` | 允许代理使用任意 Unix 套接字，而不是默认的限制集。 | | `permissions.<name>.network.dangerously_allow_non_loopback_proxy` | `boolean` | 允许为管理代理监听器提供非回环绑定地址。 | | `permissions.<name>.network.domains` | `map<string, allow | deny>` | 管理代理的域规则。使用域名或通配符模式作为键，具有 `allow` 或 `deny` 值。 | | `permissions.<name>.network.enable_socks5` | `boolean` | 当此权限配置文件启用管理网络代理时，开启 SOCKS5 监听。 | | `permissions.<name>.network.enable_socks5_udp` | `boolean` | 允许在启用时通过 SOCKS5 监听进行 UDP。 | | `permissions.<name>.network.enabled` | `boolean` | 启用此命名权限配置文件的网络访问。 | | `permissions.<name>.network.mode` | `limited | full` | 用于子进程流量的网络代理模式。 | | `permissions.<name>.network.proxy_url` | `string` | 当此权限配置文件启用管理网络代理时使用的 HTTP 代理端点。 | | `permissions.<name>.network.socks_url` | `string` | 此权限配置文件使用的 SOCKS5 代理端点。 | | `permissions.<name>.network.unix_sockets` | `map<string, allow | none>` | 管理代理的 Unix 套接字规则。使用套接字路径作为键，具有 `allow` 或 `none` 值。 | | `personality` | `none | friendly | pragmatic` | 默认交流风格，适用于声明支持 `supportsPersonality` 的模型；可以在每个线程/轮次或通过 `/personality` 进行覆盖。 | | `plan_mode_reasoning_effort` | `none | minimal | low | medium | high | xhigh` | 特定于计划模式的推理覆盖。当未设置时，计划模式使用其内置的预设默认。 | | `profile` | `string` | 启动时应用的默认配置文件（等同于 `--profile`）。 | | `profiles.<name>.*` | `various` | 对任何受支持的配置键的配置文件范围的覆盖。 | | `profiles.<name>.analytics.enabled` | `boolean` | 配置文件范围的分析启用覆盖。 | | `profiles.<name>.experimental_use_unified_exec_tool` | `boolean` | 启用统一执行的旧名称；更倾向于使用 `[features].unified_exec`。 | | `profiles.<name>.model_catalog_json` | `string (path)` | 配置文件范围的模型目录 JSON 路径覆盖（仅在启动时应用；覆盖该配置文件的顶部 `model_catalog_json`）。 | | `profiles.<name>.model_instructions_file` | `string (path)` | 配置文件范围的内置指令文件替换。 | | `profiles.<name>.oss_provider` | `lmstudio | ollama` | Configured profile’s OSS provider per `--oss` 会话。 | | `profiles.<name>.personality` | `none | friendly | pragmatic` | 支持的模型的配置文件范围的交流风格覆盖。 | | `profiles.<name>.plan_mode_reasoning_effort` | `none | minimal | low | medium | high | xhigh` | 配置文件范围的计划模式推理覆盖。 | | `profiles.<name>.service_tier` | `flex | fast` | 配置文件范围的新转弯的服务等级偏好。 | | `profiles.<name>.tools_view_image` | `boolean` | 启用或禁用该配置文件中的 `view_image` 工具。 | | `profiles.<name>.web_search` | `disabled | cached | live` | 配置文件范围的网络搜索模式覆盖（默认：`"cached"`）。 | | `profiles.<name>.windows.sandbox` | `unelevated | elevated` | 配置文件范围的 Windows 沙盒模式覆盖。 | | `project_doc_fallback_filenames` | `array<string>` | 当 `AGENTS.md` 缺失时尝试的附加文件名。 | | `project_doc_max_bytes` | `number` | 在构建项目指令时从 `AGENTS.md` 读取的最大字节数。 | | `project_root_markers` | `array<string>` | 项目根标记文件名列表；用于在搜索父目录时确定项目根。 | | `projects.<path>.trust_level` | `string` | 将项目或工作树标记为受信任或不受信任（`"trusted"` \| `"untrusted"`）。不受信任的项目跳过项目范围的 `.codex/` 层，包括项目本地配置、钩子和规则。 | | `review_model` | `string` | `/review` 使用的可选模型覆盖（默认为当前会话模型）。 | | `sandbox_mode` | `read-only | workspace-write | danger-full-access` | 在命令执行期间的文件系统和网络访问沙盒策略。 | | `sandbox_workspace_write.exclude_slash_tmp` | `boolean` | 在工作区写入模式中从可写根目录中排除 `/tmp`。 | | `sandbox_workspace_write.exclude_tmpdir_env_var` | `boolean` | 在工作区写入模式中从可写根目录中排除 `$TMPDIR`。 | | `sandbox_workspace_write.network_access` | `boolean` | 允许在工作区写入沙盒内的出站网络访问。 | | `sandbox_workspace_write.writable_roots` | `array<string>` | 当 `sandbox_mode = "workspace-write"` 时的附加可写根。 | | `service_tier` | `flex | fast` | 新转弯的首选服务分层。 | | `shell_environment_policy.exclude` | `array<string>` | 用于在默认值之后删除环境变量的全局模式。 | | `shell_environment_policy.experimental_use_profile` | `boolean` | 在生成子进程时使用用户 shell 配置文件。 | | `shell_environment_policy.ignore_default_excludes` | `boolean` | 在其他过滤器运行之前保留包含 KEY/SECRET/TOKEN 的变量。 | | `shell_environment_policy.include_only` | `array<string>` | 模式的白名单；当设置时，仅保留匹配的变量。 | | `shell_environment_policy.inherit` | `all | core | none` | 生成子进程时的基线环境继承。 | | `shell_environment_policy.set` | `map<string,string>` | 注入到每个子进程的显式环境覆盖。 | | `show_raw_agent_reasoning` | `boolean` | 在当前模型发出原始推理内容时显示它。 | | `skills.config` | `array<object>` | 存储在 config.toml 中的每个技能的启用覆盖。 | | `skills.config.<index>.enabled` | `boolean` | 启用或禁用所引用的技能。 | | `skills.config.<index>.path` | `string (path)` | 包含 `SKILL.md` 的技能文件夹的路径。 | | `sqlite_home` | `string (path)` | Codex 存储用于代理作业和其它可恢复运行时状态的 SQLite 支持状态数据库的目录。 | | `suppress_unstable_features_warning` | `boolean` | 抑制在启用开发中的功能标志时出现的警告。 | | `tool_output_token_limit` | `number` | 用于在历史中存储单个工具/函数输出的令牌预算。 | | `tool_suggest.discoverables` | `array<table>` | 允许对更多可发现连接器或插件的工具建议。每个条目使用 `type = "connector"` 或 `"plugin"` 和一个 `id`。 | | `tools.view_image` | `boolean` | 启用本地图像附件工具 `view_image`。 | | `tools.web_search` | `boolean | { context_size = "low|medium|high", allowed_domains = [string], location = { country, region, city, timezone } }` | 可选的网络搜索工具配置。旧的布尔形式仍然被接受，但对象形式允许您设置搜索上下文大小、允许的域和近似用户位置。 | | `tui` | `table` | TUI 特定选项，例如启用内联桌面通知。 | | `tui.alternate_screen` | `auto | always | never` | 控制 TUI 的备用屏幕使用情况（默认：自动；自动在 Zellij 中跳过以保留向前滚动）。 | | `tui.animations` | `boolean` | 启用终端动画（欢迎屏幕、闪烁、旋转器）（默认：true）。 | | `tui.model_availability_nux.<model>` | `integer` | 由模型标记键控的内部启动工具提示状态。 | | `tui.notification_condition` | `unfocused | always` | 控制 TUI 通知是否仅在终端未聚焦时触发或无论聚焦状态如何。默认值为 `unfocused`。 | | `tui.notification_method` | `auto | osc9 | bel` | 终端通知的通知方法（默认：自动）。 | | `tui.notifications` | `boolean | array<string>` | 启用 TUI 通知；可选限制为特定事件类型。 | | `tui.show_tooltips` | `boolean` | 显示 TUI 欢迎屏幕中的入门工具提示（默认：true）。 | | `tui.status_line` | `array<string> | null` | TUI 页脚状态行项标识符的有序列表。`null` 禁用状态行。 | | `tui.terminal_title` | `array<string> | null` | 终端窗口/标签标题项标识符的有序列表。默认值为 `["spinner", "project"]`； `null` 禁用标题更新。 | | `tui.theme` | `string` | 语法高亮主题覆盖（短横线风格主题名称）。 | | `web_search` | `disabled | cached | live` | 网络搜索模式（默认：`"cached"`；cached 使用 OpenAI 维护的索引并不获取实时页面；如果您使用 `--yolo` 或其他完全访问沙盒设置，则它默认为 `"live"`）。使用 `"live"` 获取最新的数据，使用 `"disabled"` 移除该工具。 | | `windows_wsl_setup_acknowledged` | `boolean` | 跟踪 Windows 入门确认（仅适用于 Windows）。 | | `windows.sandbox` | `unelevated | elevated` | 在 Windows 上本地运行 Codex 时的 Windows 唯一原生沙盒模式。 | | `windows.sandbox_private_desktop` | `boolean` | 在本地 Windows 上默认在私人桌面上运行最终的沙盒子进程。仅在与较旧的 `Winsta0\\Default` 行为兼容时设置为 `false`。 |
4. 键

## 实战视角
如果你会 Python，可以把这一章里的能力理解成一个可以读文件、调工具、保留状态、按规则执行的程序代理。它和 LangChain 的链、工具、记忆很像，但 Codex 更强调真实工程环境：仓库、命令、审阅、权限、并行任务。

## 给高中水平读者的最后一句话
不要急着一次学完整个体系。把每章都当成“定义 + 例子 + 适用边界”三件事去掌握，AI Agent 就会从模糊概念变成你能实际操作的工程对象。
