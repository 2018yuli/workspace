# 第55章 子代理

> 原始页面：[Subagents – Codex | OpenAI Developers](https://developers.openai.com/codex/subagents)

## 本章目标
这章面向会 Python、懂一点 LangChain、但还没有真正把 AI Agent 当成工程系统来使用的读者。重点不是背术语，而是把“这个功能为什么存在、什么时候该用、怎么安全地用”讲清楚。

## 先用一个高中数学类比
线程像解一道多步函数题时保留下来的草稿纸。后一步是否顺利，依赖前面保留下来的中间结果。

## 严谨定义
严格地说，线程是一个按时间顺序累积状态的信息序列。

## 你读完应该抓住什么
- Codex 可以通过并行生成专业代理来运行子代理工作流，然后将它们的结果整合为一个响应。这在处理复杂且高度并行的任务时尤其有用，例如代码库探索或。
- `可用性`：当前 Codex 版本默认启用子代理工作流。
- `典型工作流`：Codex 处理代理间的编排，包括生成新的子代理、路由后续指令、等待结果以及关闭代理线程。

## 分步理解
### 正文
先说直白版：
Codex 可以通过并行生成专业代理来运行子代理工作流，然后将它们的结果整合为一个响应。这在处理复杂且高度并行的任务时尤其有用，例如代码库探索或实现多步骤特性计划。
通过子代理工作流，您还可以根据任务定义自己的自定义代理，具有不同的模型配置和指令。
有关子代理工作流背后的概念和权衡，包括上下文污染、上下文衰退和模型选择指导，请参见 子代理概念。

把它理解成一个更严谨的过程：
1. Codex 可以通过并行生成专业代理来运行子代理工作流，然后将它们的结果整合为一个响应。这在处理复杂且高度并行的任务时尤其有用，例如代码库探索或实现多步骤特性计划。
2. 通过子代理工作流，您还可以根据任务定义自己的自定义代理，具有不同的模型配置和指令。
3. 有关子代理工作流背后的概念和权衡，包括上下文污染、上下文衰退和模型选择指导，请参见 子代理概念。

### 可用性
先说直白版：
当前 Codex 版本默认启用子代理工作流。
子代理活动目前在 Codex 应用和 CLI 中显示。IDE 扩展的可见性即将推出。
Codex 仅在您明确要求时才生成子代理。由于每个子代理都进行自己的模型和工具工作，子代理工作流消耗的令牌比可比的单代理运行更多。

把它理解成一个更严谨的过程：
1. 当前 Codex 版本默认启用子代理工作流。
2. 子代理活动目前在 Codex 应用和 CLI 中显示。IDE 扩展的可见性即将推出。
3. Codex 仅在您明确要求时才生成子代理。由于每个子代理都进行自己的模型和工具工作，子代理工作流消耗的令牌比可比的单代理运行更多。

### 典型工作流
先说直白版：
Codex 处理代理间的编排，包括生成新的子代理、路由后续指令、等待结果以及关闭代理线程。
当许多代理正在运行时，Codex 会等待所有请求的结果可用，然后返回一个合并的响应。
Codex 仅在您明确要求时才生成新的代理。
要查看其实际运作，请在您的项目中尝试以下提示：

把它理解成一个更严谨的过程：
1. Codex 处理代理间的编排，包括生成新的子代理、路由后续指令、等待结果以及关闭代理线程。
2. 当许多代理正在运行时，Codex 会等待所有请求的结果可用，然后返回一个合并的响应。
3. Codex 仅在您明确要求时才生成新的代理。
4. 要查看其实际运作，请在您的项目中尝试以下提示：

### 管理子代理
先说直白版：
- 在 CLI 中使用 `/agent` 在活动代理线程之间切换并检查正在进行的线程。
- 直接询问 Codex 以引导正在运行的子代理、停止它，或关闭已完成的代理线程。

把它理解成一个更严谨的过程：
1. 在 CLI 中使用 `/agent` 在活动代理线程之间切换并检查正在进行的线程。
2. 直接询问 Codex 以引导正在运行的子代理、停止它，或关闭已完成的代理线程。

### 审批和沙盒控制
先说直白版：
子代理继承您当前的沙盒策略。
在交互式 CLI 会话中，即使您正在查看主线程，来自非活跃代理线程的审批请求也可以浮出水面。审批覆盖显示源线程标签，您可以按 `o` 打开该线程，然后再批准、拒绝或回答请求。
在非交互流程中，或者每当运行无法浮出新的审批时，需要新审批的操作将失败，并且 Codex 会将错误信息返回给父工作流。
Codex 还会在生成子代理时重新应用父级轮次的实时运行时覆盖。这包括您在会话期间交互设置的沙盒和审批选择，例如 `/approvals` 变更或 `--yolo`，即使所选自定义代理文件设置了不同的默认值。

把它理解成一个更严谨的过程：
1. 子代理继承您当前的沙盒策略。
2. 在交互式 CLI 会话中，即使您正在查看主线程，来自非活跃代理线程的审批请求也可以浮出水面。审批覆盖显示源线程标签，您可以按 `o` 打开该线程，然后再批准、拒绝或回答请求。
3. 在非交互流程中，或者每当运行无法浮出新的审批时，需要新审批的操作将失败，并且 Codex 会将错误信息返回给父工作流。
4. Codex 还会在生成子代理时重新应用父级轮次的实时运行时覆盖。这包括您在会话期间交互设置的沙盒和审批选择，例如 `/approvals` 变更或 `--yolo`，即使所选自定义代理文件设置了不同的默认值。

## 实战视角
如果你会 Python，可以把这一章里的能力理解成一个可以读文件、调工具、保留状态、按规则执行的程序代理。它和 LangChain 的链、工具、记忆很像，但 Codex 更强调真实工程环境：仓库、命令、审阅、权限、并行任务。

## 给高中水平读者的最后一句话
不要急着一次学完整个体系。把每章都当成“定义 + 例子 + 适用边界”三件事去掌握，AI Agent 就会从模糊概念变成你能实际操作的工程对象。
