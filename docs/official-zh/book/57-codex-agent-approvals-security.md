# 第57章 代理批准与安全

> 原始页面：[Agent approvals & security – Codex | OpenAI Developers](https://developers.openai.com/codex/agent-approvals-security)

## 本章目标
这章面向会 Python、懂一点 LangChain、但还没有真正把 AI Agent 当成工程系统来使用的读者。重点不是背术语，而是把“这个功能为什么存在、什么时候该用、怎么安全地用”讲清楚。

## 先用一个高中数学类比
配置像给函数预先设定参数。公式不变，但参数不同，图像和输出会明显不同。

## 严谨定义
严格地说，配置是运行时行为的参数化描述。

## 你读完应该抓住什么
- Codex 帮助保护您的代码和数据，减少误用的风险。
- `沙箱和审批`：Codex 安全控制来自两个协同工作的层次：
- `网络访问 [提高风险](https://help.openai.com/articles/20001061)`：有关 Codex 云，请参见 。

## 分步理解
### 正文
先说直白版：
Codex 帮助保护您的代码和数据，减少误用的风险。
本页面涵盖了如何安全地操作 Codex，包括沙箱、审批和网络访问。如果您正在寻找用于扫描连接的 GitHub 仓库的 Codex 安全产品，请参见 Codex 安全。
默认情况下，代理在网络访问关闭的情况下运行。在本地，Codex 使用操作系统强制的沙箱，它限制它可以接触的内容（通常是当前工作区），加上一个审批政策，控制何时需要在执行操作之前停止并询问您。
有关沙箱如何在 Codex 应用程序、IDE 扩展和 CLI 中工作的高级解释，请参见 沙箱。有关更广泛的企业安全概述，请参见 Codex 安全白皮书。

把它理解成一个更严谨的过程：
1. Codex 帮助保护您的代码和数据，减少误用的风险。
2. 本页面涵盖了如何安全地操作 Codex，包括沙箱、审批和网络访问。如果您正在寻找用于扫描连接的 GitHub 仓库的 Codex 安全产品，请参见 Codex 安全。
3. 默认情况下，代理在网络访问关闭的情况下运行。在本地，Codex 使用操作系统强制的沙箱，它限制它可以接触的内容（通常是当前工作区），加上一个审批政策，控制何时需要在执行操作之前停止并询问您。
4. 有关沙箱如何在 Codex 应用程序、IDE 扩展和 CLI 中工作的高级解释，请参见 沙箱。有关更广泛的企业安全概述，请参见 Codex 安全白皮书。

### 沙箱和审批
先说直白版：
Codex 安全控制来自两个协同工作的层次：
- **沙箱模式**：Codex 在执行模型生成的命令时技术上可以做什么（例如，它可以在哪里写作，是否可以访问网络）。
- **审批政策**：当 Codex 执行某个操作时，必须在执行之前询问您（例如，离开沙箱、使用网络或运行不在可信集合中的命令）。
Codex 根据您运行它的位置使用不同的沙箱模式：

把它理解成一个更严谨的过程：
1. Codex 安全控制来自两个协同工作的层次：
2. **沙箱模式**：Codex 在执行模型生成的命令时技术上可以做什么（例如，它可以在哪里写作，是否可以访问网络）。
3. **审批政策**：当 Codex 执行某个操作时，必须在执行之前询问您（例如，离开沙箱、使用网络或运行不在可信集合中的命令）。
4. Codex 根据您运行它的位置使用不同的沙箱模式：

### 网络访问 [提高风险](https://help.openai.com/articles/20001061)
先说直白版：
有关 Codex 云，请参见 代理互联网访问 以启用完全互联网访问或域允许列表。
对于 Codex 应用、CLI 或 IDE 扩展，默认的 `workspace-write` 沙箱模式保持网络访问关闭，除非您在配置中启用它：
您还可以在不授予完全网络访问权限的情况下控制 网络搜索工具。Codex 默认使用网络搜索缓存来访问结果。缓存是 OpenAI 维护的网络结果索引，因此缓存模式返回预先索引的结果，而不是提取实时页面。这减少了来自任意实时内容的提示注入的暴露，但您仍应将网络结果视为不可信。如果您使用 `--yolo` 或其他 完全访问沙箱设置，网络搜索默认使用实时结果。使用 `--search` 或将其设置为 `"live"` 以允许实时浏览，或将其设置为 `"disabled"` 以关闭该工具：
在 Codex 中启用网络访问或网络搜索时请谨慎。提示注入可能导致代理获取并遵循不可信的指令。

把它理解成一个更严谨的过程：
1. 有关 Codex 云，请参见 代理互联网访问 以启用完全互联网访问或域允许列表。
2. 对于 Codex 应用、CLI 或 IDE 扩展，默认的 `workspace-write` 沙箱模式保持网络访问关闭，除非您在配置中启用它：
3. 您还可以在不授予完全网络访问权限的情况下控制 网络搜索工具。Codex 默认使用网络搜索缓存来访问结果。缓存是 OpenAI 维护的网络结果索引，因此缓存模式返回预先索引的结果，而不是提取实时页面。这减少了来自任意实时内容的提示注入的暴露，但您仍应将网络结果视为不可信。如果您使用 `--yolo` 或其他 完全访问沙箱设置，网络搜索默认使用实时结果。使用 `--search` 或将其设置为 `"live"` 以允许实时浏览，或将其设置为 `"disabled"` 以关闭该工具：
4. 在 Codex 中启用网络访问或网络搜索时请谨慎。提示注入可能导致代理获取并遵循不可信的指令。

### 默认值和推荐
先说直白版：
- 启动时，Codex 检测文件夹是否受版本控制，然后建议：
- 受版本控制的文件夹：`Auto`（工作区写入 + 请求时审批）
- 非版本控制的文件夹：`read-only`
- 根据您的设置，Codex 也可能在明确信任工作目录之前以 `read-only` 开始（例如，通过入职提示或 `/permissions`）。

把它理解成一个更严谨的过程：
1. 启动时，Codex 检测文件夹是否受版本控制，然后建议：
2. 受版本控制的文件夹：`Auto`（工作区写入 + 请求时审批）
3. 非版本控制的文件夹：`read-only`
4. 根据您的设置，Codex 也可能在明确信任工作目录之前以 `read-only` 开始（例如，通过入职提示或 `/permissions`）。

### 可写根中的受保护路径
先说直白版：
在默认的 `workspace-write` 沙箱策略中，可写根仍包括受保护路径：
- `<writable_root>/.git` 不论作为目录还是文件出现，都作为只读受保护。
- 如果 `<writable_root>/.git` 是一个指针文件（`gitdir: ...`），解析后的 Git 目录路径也是只读受保护。
- `<writable_root>/.agents` 在存在为目录时受保护为只读。

把它理解成一个更严谨的过程：
1. 在默认的 `workspace-write` 沙箱策略中，可写根仍包括受保护路径：
2. `<writable_root>/.git` 不论作为目录还是文件出现，都作为只读受保护。
3. 如果 `<writable_root>/.git` 是一个指针文件（`gitdir: ...`），解析后的 Git 目录路径也是只读受保护。
4. `<writable_root>/.agents` 在存在为目录时受保护为只读。

## 实战视角
如果你会 Python，可以把这一章里的能力理解成一个可以读文件、调工具、保留状态、按规则执行的程序代理。它和 LangChain 的链、工具、记忆很像，但 Codex 更强调真实工程环境：仓库、命令、审阅、权限、并行任务。

## 给高中水平读者的最后一句话
不要急着一次学完整个体系。把每章都当成“定义 + 例子 + 适用边界”三件事去掌握，AI Agent 就会从模糊概念变成你能实际操作的工程对象。
