# 第10章 子代理

> 原始页面：[Subagents – Codex | OpenAI Developers](https://developers.openai.com/codex/concepts/subagents)

## 本章目标
这章面向会 Python、懂一点 LangChain、但还没有真正把 AI Agent 当成工程系统来使用的读者。重点不是背术语，而是把“这个功能为什么存在、什么时候该用、怎么安全地用”讲清楚。

## 先用一个高中数学类比
子代理像把一道大题拆成几道小问：主问题不变，但每个小问由更专门的人分别处理。

## 严谨定义
严格地说，子代理是主代理派生出的局部求解器，拥有较小的任务域和更清晰的边界。

## 你读完应该抓住什么
- Codex 可以通过并行生成专门的代理来运行子代理工作流，以便它们可以同时探索、处理或分析工作。
- `为什么子代理工作流有帮助`：即使有较大的上下文窗口，模型也是有限的。如果你用噪音的中间输出（如探索笔记、测试日志、堆栈跟踪和命令输出）淹没主对。
- `核心术语`：Codex 在子代理工作流中使用几个相关术语：

## 分步理解
### 正文
先说直白版：
Codex 可以通过并行生成专门的代理来运行子代理工作流，以便它们可以同时探索、处理或分析工作。
本页面解释了核心概念和权衡。有关设置、代理配置和示例，请参见 子代理。

把它理解成一个更严谨的过程：
1. Codex 可以通过并行生成专门的代理来运行子代理工作流，以便它们可以同时探索、处理或分析工作。
2. 本页面解释了核心概念和权衡。有关设置、代理配置和示例，请参见 子代理。

### 为什么子代理工作流有帮助
先说直白版：
即使有较大的上下文窗口，模型也是有限的。如果你用噪音的中间输出（如探索笔记、测试日志、堆栈跟踪和命令输出）淹没主对话（你在其中定义要求、约束和决策），会导致会话随着时间的推移变得不可靠。
这通常被描述为：
- **上下文污染**：有用的信息被埋没在嘈杂的中间输出之下。
- **上下文腐烂**：随着对话被不太相关的细节填充，性能下降。

把它理解成一个更严谨的过程：
1. 即使有较大的上下文窗口，模型也是有限的。如果你用噪音的中间输出（如探索笔记、测试日志、堆栈跟踪和命令输出）淹没主对话（你在其中定义要求、约束和决策），会导致会话随着时间的推移变得不可靠。
2. 这通常被描述为：
3. **上下文污染**：有用的信息被埋没在嘈杂的中间输出之下。
4. **上下文腐烂**：随着对话被不太相关的细节填充，性能下降。

### 核心术语
先说直白版：
Codex 在子代理工作流中使用几个相关术语：
- **子代理工作流**：Codex 运行并行代理并组合其结果的工作流。
- **子代理**：Codex 启动以处理特定任务的代理。
- **代理线程**：代理的 CLI 线程，你可以通过 `/agent` 检查并在其间切换。

把它理解成一个更严谨的过程：
1. Codex 在子代理工作流中使用几个相关术语：
2. **子代理工作流**：Codex 运行并行代理并组合其结果的工作流。
3. **子代理**：Codex 启动以处理特定任务的代理。
4. **代理线程**：代理的 CLI 线程，你可以通过 `/agent` 检查并在其间切换。

### 触发子代理工作流
先说直白版：
Codex 不会自动生成子代理，只有在你明确要求子代理或并行代理工作时才应使用子代理。
实际上，手动触发意味着使用直接指令，例如“生成两个代理”、“并行分配此工作”或“一点使用一个代理”。子代理工作流消耗的令牌比可比的单代理运行更多，因为每个子代理都进行自己的模型和工具工作。
一个好的子代理提示应该解释如何划分工作，Codex 是否应在继续之前等待所有代理，以及应返回的摘要或输出。

把它理解成一个更严谨的过程：
1. Codex 不会自动生成子代理，只有在你明确要求子代理或并行代理工作时才应使用子代理。
2. 实际上，手动触发意味着使用直接指令，例如“生成两个代理”、“并行分配此工作”或“一点使用一个代理”。子代理工作流消耗的令牌比可比的单代理运行更多，因为每个子代理都进行自己的模型和工具工作。
3. 一个好的子代理提示应该解释如何划分工作，Codex 是否应在继续之前等待所有代理，以及应返回的摘要或输出。

### 选择模型和推理
先说直白版：
不同的代理需要不同的模型和推理设置。
如果你不固定模型或 `model_reasoning_effort`，Codex 可以选择一种平衡任务的智能、速度和价格的设置。它可能偏爱 `gpt-5.4-mini` 用于快速扫描，或在该模型可用时更高努力的 `gpt-5.5` 配置以进行更具挑战性的推理。当你需要更精细的控制时，可以在提示中引导该选择，或直接在代理文件中设置 `model` 和 `model_reasoning_effort`。
对于 Codex 中的大多数任务，当 `gpt-5.5` 可用时，请从 `gpt-5.5` 开始。如果 `gpt-5.5` 尚不可用，请在推送期间继续使用 `gpt-5.4`。当你希望为较轻的子代理工作寻求更快、更低成本的选项时，请使用 `gpt-5.4-mini`。如果你拥有 ChatGPT Pro 并希望进行近乎即时的文本迭代，`gpt-5.3-codex-spark` 在研究预览中仍然可用。

把它理解成一个更严谨的过程：
1. 不同的代理需要不同的模型和推理设置。
2. 如果你不固定模型或 `model_reasoning_effort`，Codex 可以选择一种平衡任务的智能、速度和价格的设置。它可能偏爱 `gpt-5.4-mini` 用于快速扫描，或在该模型可用时更高努力的 `gpt-5.5` 配置以进行更具挑战性的推理。当你需要更精细的控制时，可以在提示中引导该选择，或直接在代理文件中设置 `model` 和 `model_reasoning_effort`。
3. 对于 Codex 中的大多数任务，当 `gpt-5.5` 可用时，请从 `gpt-5.5` 开始。如果 `gpt-5.5` 尚不可用，请在推送期间继续使用 `gpt-5.4`。当你希望为较轻的子代理工作寻求更快、更低成本的选项时，请使用 `gpt-5.4-mini`。如果你拥有 ChatGPT Pro 并希望进行近乎即时的文本迭代，`gpt-5.3-codex-spark` 在研究预览中仍然可用。

## 实战视角
如果你会 Python，可以把这一章里的能力理解成一个可以读文件、调工具、保留状态、按规则执行的程序代理。它和 LangChain 的链、工具、记忆很像，但 Codex 更强调真实工程环境：仓库、命令、审阅、权限、并行任务。

## 给高中水平读者的最后一句话
不要急着一次学完整个体系。把每章都当成“定义 + 例子 + 适用边界”三件事去掌握，AI Agent 就会从模糊概念变成你能实际操作的工程对象。
