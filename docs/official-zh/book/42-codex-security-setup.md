# 第42章 设置

> 原始页面：[Setup – Codex Security | OpenAI Developers](https://developers.openai.com/codex/security/setup)

## 本章目标
这章面向会 Python、懂一点 LangChain、但还没有真正把 AI Agent 当成工程系统来使用的读者。重点不是背术语，而是把“这个功能为什么存在、什么时候该用、怎么安全地用”讲清楚。

## 先用一个高中数学类比
安全像不等式约束。你不是只关心最优解，还要保证所有可行解都落在安全区域内。

## 严谨定义
严格地说，安全机制是对代理行为加入的一组风险边界与审计条件。

## 你读完应该抓住什么
- 本页面指导您从初始访问到审查发现和 Codex 安全中的修复请求。
- `1\. 访问和环境`：Codex 安全扫描通过 Codex Cloud 连接的 GitHub 仓库。
- `2\. 新的安全扫描`：环境存在后，访问 创建安全扫描 并选择您刚刚连接的仓库。

## 分步理解
### 正文
先说直白版：
本页面指导您从初始访问到审查发现和 Codex 安全中的修复请求。
首先确认您已设置 Codex Cloud。如果没有，请参阅 Codex\\ Cloud 开始。

把它理解成一个更严谨的过程：
1. 本页面指导您从初始访问到审查发现和 Codex 安全中的修复请求。
2. 首先确认您已设置 Codex Cloud。如果没有，请参阅 Codex\\ Cloud 开始。

### 1\. 访问和环境
先说直白版：
Codex 安全扫描通过 Codex Cloud 连接的 GitHub 仓库。
- 确认您的工作区可以访问 Codex 安全。
- 确认您要扫描的仓库在 Codex Cloud 中可用。
访问 Codex 环境 并检查仓库是否已有环境。如果没有，请在继续之前创建一个。

把它理解成一个更严谨的过程：
1. Codex 安全扫描通过 Codex Cloud 连接的 GitHub 仓库。
2. 确认您的工作区可以访问 Codex 安全。
3. 确认您要扫描的仓库在 Codex Cloud 中可用。
4. 访问 Codex 环境 并检查仓库是否已有环境。如果没有，请在继续之前创建一个。

### 2\. 新的安全扫描
先说直白版：
环境存在后，访问 创建安全扫描 并选择您刚刚连接的仓库。
Codex 安全首先从最新提交向后扫描仓库。它利用这些信息在新的提交到达时构建和刷新扫描上下文。
要配置一个仓库：
1. 选择 GitHub 组织。 2. 选择仓库。 3. 选择您要扫描的分支。 4. 选择环境。 5. 选择一个 **历史时间窗口**。较长的窗口提供更多上下文，但回填所需时间较长。 6. 点击 **创建**。

把它理解成一个更严谨的过程：
1. 环境存在后，访问 创建安全扫描 并选择您刚刚连接的仓库。
2. Codex 安全首先从最新提交向后扫描仓库。它利用这些信息在新的提交到达时构建和刷新扫描上下文。
3. 要配置一个仓库：
4. 1. 选择 GitHub 组织。 2. 选择仓库。 3. 选择您要扫描的分支。 4. 选择环境。 5. 选择一个 **历史时间窗口**。较长的窗口提供更多上下文，但回填所需时间较长。 6. 点击 **创建**。

### 3\. 初始扫描可能需要一些时间
先说直白版：
当您创建扫描时，Codex 安全首先在所选的历史时间窗口内进行提交级别的安全检查。 初始回填可能需要几个小时，尤其是对于较大的仓库或较长的时间窗口。 如果发现内容没有立即显示，这是正常现象。在打开工单或故障排除之前，请等待初始扫描完成。
初始扫描设置自动且全面。这可能需要几个小时。如果第一次发现的结果延迟出现，请不要惊慌。

把它理解成一个更严谨的过程：
1. 当您创建扫描时，Codex 安全首先在所选的历史时间窗口内进行提交级别的安全检查。 初始回填可能需要几个小时，尤其是对于较大的仓库或较长的时间窗口。 如果发现内容没有立即显示，这是正常现象。在打开工单或故障排除之前，请等待初始扫描完成。
2. 初始扫描设置自动且全面。这可能需要几个小时。如果第一次发现的结果延迟出现，请不要惊慌。

### 4\. 审查扫描并改善威胁模型
先说直白版：
当初始扫描结束后，打开扫描并审查生成的威胁模型。 在初始发现出现后，更新威胁模型以匹配您的架构、信任边界和业务上下文。 这有助于 Codex 安全为您的团队排名问题。
如果您希望扫描结果发生变化，您可以使用更新后的范围、优先事项和假设编辑威胁模型。
在初始发现出现后，重新查看模型，以确保扫描指导与当前优先事项保持一致。 保持最新有助于 Codex 安全提出更好的建议。
有关威胁模型及其如何影响重要性和分类的更深层次解释，请参阅 改进威胁模型。

把它理解成一个更严谨的过程：
1. 当初始扫描结束后，打开扫描并审查生成的威胁模型。 在初始发现出现后，更新威胁模型以匹配您的架构、信任边界和业务上下文。 这有助于 Codex 安全为您的团队排名问题。
2. 如果您希望扫描结果发生变化，您可以使用更新后的范围、优先事项和假设编辑威胁模型。
3. 在初始发现出现后，重新查看模型，以确保扫描指导与当前优先事项保持一致。 保持最新有助于 Codex 安全提出更好的建议。
4. 有关威胁模型及其如何影响重要性和分类的更深层次解释，请参阅 改进威胁模型。

## 实战视角
如果你会 Python，可以把这一章里的能力理解成一个可以读文件、调工具、保留状态、按规则执行的程序代理。它和 LangChain 的链、工具、记忆很像，但 Codex 更强调真实工程环境：仓库、命令、审阅、权限、并行任务。

## 给高中水平读者的最后一句话
不要急着一次学完整个体系。把每章都当成“定义 + 例子 + 适用边界”三件事去掌握，AI Agent 就会从模糊概念变成你能实际操作的工程对象。
