# 第06章 自定义

> 原始页面：[Customization – Codex | OpenAI Developers](https://developers.openai.com/codex/concepts/customization)

## 本章目标
这章面向会 Python、懂一点 LangChain、但还没有真正把 AI Agent 当成工程系统来使用的读者。重点不是背术语，而是把“这个功能为什么存在、什么时候该用、怎么安全地用”讲清楚。

## 先用一个高中数学类比
把这一章看成在学习一个新的数学对象。先认识它的定义，再看它的性质，最后看它怎么解题。

## 严谨定义
严格地说，这一章讨论的是 Codex 体系中的一个功能模块，以及它与输入、状态、输出之间的关系。

## 你读完应该抓住什么
- 定制化是指如何让 Codex 按照你的团队的工作方式运行。
- `AGENTS 指导`：`AGENTS.md` 为 Codex 提供持久的项目指导，这些指导与您的代码库一起移动并在代理开始工作之前应用。保持简。
- `何时更新 `AGENTS.md``：- **重复错误**：如果代理反复犯同样的错误，请添加一条规则。

## 分步理解
### 正文
先说直白版：
定制化是指如何让 Codex 按照你的团队的工作方式运行。
在 Codex 中，定制化来自几个相互协作的层次：
- **项目指导（`AGENTS.md`）** 用于持久性指令
- **记忆** 用于从过去工作中学习的有用上下文

把它理解成一个更严谨的过程：
1. 定制化是指如何让 Codex 按照你的团队的工作方式运行。
2. 在 Codex 中，定制化来自几个相互协作的层次：
3. **项目指导（`AGENTS.md`）** 用于持久性指令
4. **记忆** 用于从过去工作中学习的有用上下文

### AGENTS 指导
先说直白版：
`AGENTS.md` 为 Codex 提供持久的项目指导，这些指导与您的代码库一起移动并在代理开始工作之前应用。保持简短。
将其用于您希望 Codex 每次在代码库中遵循的规则，例如：
- 构建和测试命令
- 评审期望

把它理解成一个更严谨的过程：
1. `AGENTS.md` 为 Codex 提供持久的项目指导，这些指导与您的代码库一起移动并在代理开始工作之前应用。保持简短。
2. 将其用于您希望 Codex 每次在代码库中遵循的规则，例如：
3. 构建和测试命令
4. 评审期望

### 何时更新 `AGENTS.md`
先说直白版：
- **重复错误**：如果代理反复犯同样的错误，请添加一条规则。
- **阅读过多**：如果它找到正确的文件但阅读了太多文档，请添加路由指导（优先考虑哪些目录/文件）。
- **重复的 PR 反馈**：如果您多次留下相同的反馈，请将其编纂。
- **在 GitHub 中**：在拉取请求评论中，标记 `@codex` 并请求（例如，`@codex add this to AGENTS.md`）将更新委派到云任务。

把它理解成一个更严谨的过程：
1. **重复错误**：如果代理反复犯同样的错误，请添加一条规则。
2. **阅读过多**：如果它找到正确的文件但阅读了太多文档，请添加路由指导（优先考虑哪些目录/文件）。
3. **重复的 PR 反馈**：如果您多次留下相同的反馈，请将其编纂。
4. **在 GitHub 中**：在拉取请求评论中，标记 `@codex` 并请求（例如，`@codex add this to AGENTS.md`）将更新委派到云任务。

### 技能
先说直白版：
技能为 Codex 提供可重用的能力以实现可重复的工作流程。技能通常是可重用工作流程的最佳选择，因为它们支持更丰富的指令、脚本和引用，同时保持跨任务的可重用性。技能被加载并对代理可见（至少它们的元数据），因此 Codex 可以隐式发现和选择它们。这使得丰富的工作流程可用，而不会提前膨胀上下文。
使用技能文件夹在本地撰写和迭代工作流程。如果该工作流程已经存在插件，则先安装它以重用经过验证的设置。当您希望跨团队分发自己的工作流程或将其与应用程序集成捆绑时，将其打包为 插件。技能仍然是创作格式；插件是可安装的分发单元。
技能通常是一个 `SKILL.md` 文件以及可选的脚本、引用和资产。
- my-skill/

把它理解成一个更严谨的过程：
1. 技能为 Codex 提供可重用的能力以实现可重复的工作流程。技能通常是可重用工作流程的最佳选择，因为它们支持更丰富的指令、脚本和引用，同时保持跨任务的可重用性。技能被加载并对代理可见（至少它们的元数据），因此 Codex 可以隐式发现和选择它们。这使得丰富的工作流程可用，而不会提前膨胀上下文。
2. 使用技能文件夹在本地撰写和迭代工作流程。如果该工作流程已经存在插件，则先安装它以重用经过验证的设置。当您希望跨团队分发自己的工作流程或将其与应用程序集成捆绑时，将其打包为 插件。技能仍然是创作格式；插件是可安装的分发单元。
3. 技能通常是一个 `SKILL.md` 文件以及可选的脚本、引用和资产。
4. my-skill/

### MCP
先说直白版：
MCP（模型上下文协议）是将 Codex 连接到外部工具和上下文提供者的标准方法。它对于远程托管的系统尤为有用，例如 Figma、Linear、GitHub 或您的团队所依赖的内部知识服务。
当 Codex 需要本地代码库之外的功能时（例如问题跟踪器、设计工具、浏览器或共享文档系统），请使用 MCP。
可以这样思考：
- **主机**：Codex

把它理解成一个更严谨的过程：
1. MCP（模型上下文协议）是将 Codex 连接到外部工具和上下文提供者的标准方法。它对于远程托管的系统尤为有用，例如 Figma、Linear、GitHub 或您的团队所依赖的内部知识服务。
2. 当 Codex 需要本地代码库之外的功能时（例如问题跟踪器、设计工具、浏览器或共享文档系统），请使用 MCP。
3. 可以这样思考：
4. **主机**：Codex

## 实战视角
如果你会 Python，可以把这一章里的能力理解成一个可以读文件、调工具、保留状态、按规则执行的程序代理。它和 LangChain 的链、工具、记忆很像，但 Codex 更强调真实工程环境：仓库、命令、审阅、权限、并行任务。

## 给高中水平读者的最后一句话
不要急着一次学完整个体系。把每章都当成“定义 + 例子 + 适用边界”三件事去掌握，AI Agent 就会从模糊概念变成你能实际操作的工程对象。
