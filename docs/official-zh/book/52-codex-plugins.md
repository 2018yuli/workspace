# 第52章 概述

> 原始页面：[Plugins – Codex | OpenAI Developers](https://developers.openai.com/codex/plugins)

## 本章目标
这章面向会 Python、懂一点 LangChain、但还没有真正把 AI Agent 当成工程系统来使用的读者。重点不是背术语，而是把“这个功能为什么存在、什么时候该用、怎么安全地用”讲清楚。

## 先用一个高中数学类比
工作流像标准解题模板。先审题，再列式，再求解，再验算。

## 严谨定义
严格地说，工作流是多个步骤之间按依赖关系构成的执行图。

## 你读完应该抓住什么
- `概述`：插件将技能、应用集成和 MCP 服务器捆绑成可重用的工作流，以供 Codex 使用。
- `Codex 应用中的插件目录`：在 Codex 应用中打开 **插件** 以浏览和安装经过策划的插件。
- `CLI 中的插件目录`：在 Codex CLI 中，运行以下命令以打开插件列表：

## 分步理解
### 概述
先说直白版：
插件将技能、应用集成和 MCP 服务器捆绑成可重用的工作流，以供 Codex 使用。
扩展 Codex 的功能，例如：
- 安装 Gmail 插件使 Codex 能够读取和管理 Gmail。
- 安装 Google Drive 插件以在 Drive、Docs、Sheets 和 Slides 之间工作。

把它理解成一个更严谨的过程：
1. 插件将技能、应用集成和 MCP 服务器捆绑成可重用的工作流，以供 Codex 使用。
2. 扩展 Codex 的功能，例如：
3. 安装 Gmail 插件使 Codex 能够读取和管理 Gmail。
4. 安装 Google Drive 插件以在 Drive、Docs、Sheets 和 Slides 之间工作。

### Codex 应用中的插件目录
先说直白版：
在 Codex 应用中打开 **插件** 以浏览和安装经过策划的插件。

把它理解成一个更严谨的过程：
1. 在 Codex 应用中打开 **插件** 以浏览和安装经过策划的插件。

### CLI 中的插件目录
先说直白版：
在 Codex CLI 中，运行以下命令以打开插件列表：
CLI 插件浏览器按市场对插件进行分组。使用市场选项卡切换源，打开插件以检查详细信息，并按 `Space` 切换已安装插件的启用状态。

把它理解成一个更严谨的过程：
1. 在 Codex CLI 中，运行以下命令以打开插件列表：
2. CLI 插件浏览器按市场对插件进行分组。使用市场选项卡切换源，打开插件以检查详细信息，并按 `Space` 切换已安装插件的启用状态。

### 安装和使用插件
先说直白版：
打开插件目录后：
1. 搜索或浏览插件，然后打开其详细信息。 2. 选择安装按钮。在应用中，选择加号按钮或 **添加到 Codex**。在 CLI 中，选择 `安装插件`。 3. 如果插件需要外部应用，请在提示时连接。如果一些插件在安装过程中要求您进行身份验证，其他插件会等待您第一次使用它们时再进行。 4. 安装后，启动新线程并要求 Codex 使用该插件。
安装插件后，您可以直接在提示窗口中使用它：
直接描述任务

把它理解成一个更严谨的过程：
1. 打开插件目录后：
2. 1. 搜索或浏览插件，然后打开其详细信息。 2. 选择安装按钮。在应用中，选择加号按钮或 **添加到 Codex**。在 CLI 中，选择 `安装插件`。 3. 如果插件需要外部应用，请在提示时连接。如果一些插件在安装过程中要求您进行身份验证，其他插件会等待您第一次使用它们时再进行。 4. 安装后，启动新线程并要求 Codex 使用该插件。
3. 安装插件后，您可以直接在提示窗口中使用它：
4. 直接描述任务

### 权限和数据共享工作的方式
先说直白版：
安装插件使其工作流可用于 Codex，但您现有的 批准设置 仍然适用。任何连接的外部服务仍受其自身身份验证、隐私和数据共享政策的约束。
- 捆绑的技能在您安装插件后立即可用。
- 如果插件包含应用，Codex 可能会在设置期间或第一次使用时提示您在 ChatGPT 中安装或登录这些应用。
- 如果插件包含 MCP 服务器，它们可能需要额外的设置或身份验证才能使用。

把它理解成一个更严谨的过程：
1. 安装插件使其工作流可用于 Codex，但您现有的 批准设置 仍然适用。任何连接的外部服务仍受其自身身份验证、隐私和数据共享政策的约束。
2. 捆绑的技能在您安装插件后立即可用。
3. 如果插件包含应用，Codex 可能会在设置期间或第一次使用时提示您在 ChatGPT 中安装或登录这些应用。
4. 如果插件包含 MCP 服务器，它们可能需要额外的设置或身份验证才能使用。

## 实战视角
如果你会 Python，可以把这一章里的能力理解成一个可以读文件、调工具、保留状态、按规则执行的程序代理。它和 LangChain 的链、工具、记忆很像，但 Codex 更强调真实工程环境：仓库、命令、审阅、权限、并行任务。

## 给高中水平读者的最后一句话
不要急着一次学完整个体系。把每章都当成“定义 + 例子 + 适用边界”三件事去掌握，AI Agent 就会从模糊概念变成你能实际操作的工程对象。
