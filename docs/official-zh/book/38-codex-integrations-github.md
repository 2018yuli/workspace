# 第38章 GitHub

> 原始页面：[Use Codex in GitHub | OpenAI Developers](https://developers.openai.com/codex/integrations/github)

## 本章目标
这章面向会 Python、懂一点 LangChain、但还没有真正把 AI Agent 当成工程系统来使用的读者。重点不是背术语，而是把“这个功能为什么存在、什么时候该用、怎么安全地用”讲清楚。

## 先用一个高中数学类比
把这一章看成在学习一个新的数学对象。先认识它的定义，再看它的性质，最后看它怎么解题。

## 严谨定义
严格地说，这一章讨论的是 Codex 体系中的一个功能模块，以及它与输入、状态、输出之间的关系。

## 你读完应该抓住什么
- 使用 Codex 审查拉取请求，而无需离开 GitHub。添加一个拉取请求评论，输入 `@codex review`，Codex 会回复一个标准。
- `设置代码审查`：1. 设置 Codex 云。 2. 转到 Codex 设置，为您的存储库启用 **代码审查**。
- `请求审查`：1. 在拉取请求评论中，提及 `@codex review`。 2. 等待 Codex 响应（👀）并发布审查。

## 分步理解
### 正文
先说直白版：
使用 Codex 审查拉取请求，而无需离开 GitHub。添加一个拉取请求评论，输入 `@codex review`，Codex 会回复一个标准的 GitHub 代码审查。

把它理解成一个更严谨的过程：
1. 使用 Codex 审查拉取请求，而无需离开 GitHub。添加一个拉取请求评论，输入 `@codex review`，Codex 会回复一个标准的 GitHub 代码审查。

### 设置代码审查
先说直白版：
1. 设置 Codex 云。 2. 转到 Codex 设置，为您的存储库启用 **代码审查**。

把它理解成一个更严谨的过程：
1. 1. 设置 Codex 云。 2. 转到 Codex 设置，为您的存储库启用 **代码审查**。

### 请求审查
先说直白版：
1. 在拉取请求评论中，提及 `@codex review`。 2. 等待 Codex 响应（👀）并发布审查。
Codex 在拉取请求上发布审查，就像队友一样。

把它理解成一个更严谨的过程：
1. 1. 在拉取请求评论中，提及 `@codex review`。 2. 等待 Codex 响应（👀）并发布审查。
2. Codex 在拉取请求上发布审查，就像队友一样。

### 启用自动审查
先说直白版：
如果您希望 Codex 自动审查每个拉取请求，请在 Codex 设置中启用 **自动审查**。Codex 每当有新的 PR 提交审查时，都会发布一个审查，而不需要 `@codex review` 评论。

把它理解成一个更严谨的过程：
1. 如果您希望 Codex 自动审查每个拉取请求，请在 Codex 设置中启用 **自动审查**。Codex 每当有新的 PR 提交审查时，都会发布一个审查，而不需要 `@codex review` 评论。

### 自定义 Codex 审查内容
先说直白版：
Codex 在您的存储库中搜索 `AGENTS.md` 文件，并遵循您包含的任何 **审查指南**。
要为存储库设置指南，请添加或更新一个顶层的 `AGENTS.md`，包括如下部分：

把它理解成一个更严谨的过程：
1. Codex 在您的存储库中搜索 `AGENTS.md` 文件，并遵循您包含的任何 **审查指南**。
2. 要为存储库设置指南，请添加或更新一个顶层的 `AGENTS.md`，包括如下部分：

## 实战视角
如果你会 Python，可以把这一章里的能力理解成一个可以读文件、调工具、保留状态、按规则执行的程序代理。它和 LangChain 的链、工具、记忆很像，但 Codex 更强调真实工程环境：仓库、命令、审阅、权限、并行任务。

## 给高中水平读者的最后一句话
不要急着一次学完整个体系。把每章都当成“定义 + 例子 + 适用边界”三件事去掌握，AI Agent 就会从模糊概念变成你能实际操作的工程对象。
