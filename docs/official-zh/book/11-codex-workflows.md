# 第11章 工作流

> 原始页面：[Workflows – Codex | OpenAI Developers](https://developers.openai.com/codex/workflows)

## 本章目标
这章面向会 Python、懂一点 LangChain、但还没有真正把 AI Agent 当成工程系统来使用的读者。重点不是背术语，而是把“这个功能为什么存在、什么时候该用、怎么安全地用”讲清楚。

## 先用一个高中数学类比
把提示词想成做几何证明时的题目条件。条件越完整，证明路径越短；条件越含糊，辅助线就会乱加。

## 严谨定义
严格地说，提示是对目标函数、约束条件和验证标准的联合描述。

## 你读完应该抓住什么
- Codex 在您将其视为一个具有明确上下文和“完成”清晰定义的队友时效果最佳。 此页面提供了 Codex IDE 扩展、Codex CLI 和 。
- `如何阅读这些示例`：每个工作流包括：
- `解释代码库`：在您入职、继承服务或试图理解协议、数据模型或请求流程时使用此功能。

## 分步理解
### 正文
先说直白版：
Codex 在您将其视为一个具有明确上下文和“完成”清晰定义的队友时效果最佳。 此页面提供了 Codex IDE 扩展、Codex CLI 和 Codex 云的端到端工作流示例。
如果您是 Codex 的新手，请先阅读 提示，然后再回来查看具体配方。

把它理解成一个更严谨的过程：
1. Codex 在您将其视为一个具有明确上下文和“完成”清晰定义的队友时效果最佳。 此页面提供了 Codex IDE 扩展、Codex CLI 和 Codex 云的端到端工作流示例。
2. 如果您是 Codex 的新手，请先阅读 提示，然后再回来查看具体配方。

### 如何阅读这些示例
先说直白版：
每个工作流包括：
- **何时使用**以及哪个 Codex 表面最合适（IDE、CLI 或云）。
- **步骤**，带有示例用户提示。
- **上下文说明**：Codex 自动看到的内容与您需要附加的内容。

把它理解成一个更严谨的过程：
1. 每个工作流包括：
2. **何时使用**以及哪个 Codex 表面最合适（IDE、CLI 或云）。
3. **步骤**，带有示例用户提示。
4. **上下文说明**：Codex 自动看到的内容与您需要附加的内容。

### 解释代码库
先说直白版：
在您入职、继承服务或试图理解协议、数据模型或请求流程时使用此功能。

把它理解成一个更严谨的过程：
1. 在您入职、继承服务或试图理解协议、数据模型或请求流程时使用此功能。

### IDE 扩展工作流（适合本地探索）
先说直白版：
1. 打开最相关的文件。
2. 选择您关心的代码（可选，但推荐）。
3. 提示 Codex：
包括：

把它理解成一个更严谨的过程：
1. 1. 打开最相关的文件。
2. 2. 选择您关心的代码（可选，但推荐）。
3. 3. 提示 Codex：
4. 包括：

### CLI 工作流（在您希望获得转录 + shell 命令时）
先说直白版：
1. 开始一个交互式会话：
2. 附加文件（可选）并提示：
上下文说明：
- 您可以在 composer 中使用 `@` 插入来自工作区的文件路径，或者 `/mention` 附加特定文件。

把它理解成一个更严谨的过程：
1. 1. 开始一个交互式会话：
2. 2. 附加文件（可选）并提示：
3. 上下文说明：
4. 您可以在 composer 中使用 `@` 插入来自工作区的文件路径，或者 `/mention` 附加特定文件。

## 实战视角
如果你会 Python，可以把这一章里的能力理解成一个可以读文件、调工具、保留状态、按规则执行的程序代理。它和 LangChain 的链、工具、记忆很像，但 Codex 更强调真实工程环境：仓库、命令、审阅、权限、并行任务。

## 给高中水平读者的最后一句话
不要急着一次学完整个体系。把每章都当成“定义 + 例子 + 适用边界”三件事去掌握，AI Agent 就会从模糊概念变成你能实际操作的工程对象。
