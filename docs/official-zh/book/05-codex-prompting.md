# 第05章 提示

> 原始页面：[Prompting – Codex | OpenAI Developers](https://developers.openai.com/codex/prompting)

## 本章目标
这章面向会 Python、懂一点 LangChain、但还没有真正把 AI Agent 当成工程系统来使用的读者。重点不是背术语，而是把“这个功能为什么存在、什么时候该用、怎么安全地用”讲清楚。

## 先用一个高中数学类比
把提示词想成做几何证明时的题目条件。条件越完整，证明路径越短；条件越含糊，辅助线就会乱加。

## 严谨定义
严格地说，提示是对目标函数、约束条件和验证标准的联合描述。

## 你读完应该抓住什么
- `提示`：你通过发送提示（用户消息）与 Codex 互动，这些提示描述你希望它做什么。
- `线程`：线程是一个单一会话：你的提示加上随后模型输出和工具调用。一个线程可以包含多个提示。例如，你的第一个提示可能要求 Codex 实现一个功。
- `上下文`：当你提交一个提示时，包含 Codex 可以使用的上下文，例如对相关文件和图像的引用。Codex IDE 扩展自动包括打开文件的列表和。

## 分步理解
### 提示
先说直白版：
你通过发送提示（用户消息）与 Codex 互动，这些提示描述你希望它做什么。
示例提示：
当你提交一个提示时，Codex 在一个循环中工作：它调用模型，然后执行模型输出所指示的操作，例如文件读取、文件编辑和工具调用。此过程在任务完成或你取消它时结束。
与 ChatGPT 一样，Codex 的有效性取决于你给它的指令。以下是一些我们在提示 Codex 时发现的有用提示：

把它理解成一个更严谨的过程：
1. 你通过发送提示（用户消息）与 Codex 互动，这些提示描述你希望它做什么。
2. 示例提示：
3. 当你提交一个提示时，Codex 在一个循环中工作：它调用模型，然后执行模型输出所指示的操作，例如文件读取、文件编辑和工具调用。此过程在任务完成或你取消它时结束。
4. 与 ChatGPT 一样，Codex 的有效性取决于你给它的指令。以下是一些我们在提示 Codex 时发现的有用提示：

### 线程
先说直白版：
线程是一个单一会话：你的提示加上随后模型输出和工具调用。一个线程可以包含多个提示。例如，你的第一个提示可能要求 Codex 实现一个功能，而后续提示可能要求它添加测试。
当 Codex 正在积极处理时，线程被称为“运行中”。你可以同时运行多个线程，但应避免让两个线程修改同一文件。你也可以稍后通过另一个提示继续运行一个线程。
线程可以在本地或云中运行：
- **本地线程** 在你的机器上运行。Codex 可以读取和编辑你的文件并运行命令，因此你可以看到所做的更改并使用你现有的工具。为了降低在工作区之外意外更改的风险，本地线程在 沙箱 中运行。

把它理解成一个更严谨的过程：
1. 线程是一个单一会话：你的提示加上随后模型输出和工具调用。一个线程可以包含多个提示。例如，你的第一个提示可能要求 Codex 实现一个功能，而后续提示可能要求它添加测试。
2. 当 Codex 正在积极处理时，线程被称为“运行中”。你可以同时运行多个线程，但应避免让两个线程修改同一文件。你也可以稍后通过另一个提示继续运行一个线程。
3. 线程可以在本地或云中运行：
4. **本地线程** 在你的机器上运行。Codex 可以读取和编辑你的文件并运行命令，因此你可以看到所做的更改并使用你现有的工具。为了降低在工作区之外意外更改的风险，本地线程在 沙箱 中运行。

### 上下文
先说直白版：
当你提交一个提示时，包含 Codex 可以使用的上下文，例如对相关文件和图像的引用。Codex IDE 扩展自动包括打开文件的列表和选定文本范围作为上下文。
当代理工作时，它还会从文件内容、工具输出和它所做的事情及仍需做的事情的持续记录中收集上下文。
线程中的所有信息必须适合模型的 **上下文窗口**，这个窗口因模型而异。Codex 会监控并报告剩余空间。对于较长的任务，Codex 可能会通过总结相关信息并丢弃不相关的细节来自动 **压缩** 上下文。通过重复压缩，Codex 可以在多个步骤中继续处理复杂任务。

把它理解成一个更严谨的过程：
1. 当你提交一个提示时，包含 Codex 可以使用的上下文，例如对相关文件和图像的引用。Codex IDE 扩展自动包括打开文件的列表和选定文本范围作为上下文。
2. 当代理工作时，它还会从文件内容、工具输出和它所做的事情及仍需做的事情的持续记录中收集上下文。
3. 线程中的所有信息必须适合模型的 **上下文窗口**，这个窗口因模型而异。Codex 会监控并报告剩余空间。对于较长的任务，Codex 可能会通过总结相关信息并丢弃不相关的细节来自动 **压缩** 上下文。通过重复压缩，Codex 可以在多个步骤中继续处理复杂任务。

## 实战视角
如果你会 Python，可以把这一章里的能力理解成一个可以读文件、调工具、保留状态、按规则执行的程序代理。它和 LangChain 的链、工具、记忆很像，但 Codex 更强调真实工程环境：仓库、命令、审阅、权限、并行任务。

## 给高中水平读者的最后一句话
不要急着一次学完整个体系。把每章都当成“定义 + 例子 + 适用边界”三件事去掌握，AI Agent 就会从模糊概念变成你能实际操作的工程对象。
