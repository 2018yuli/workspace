# 第15章 功能

> 原始页面：[Features – Codex app | OpenAI Developers](https://developers.openai.com/codex/app/features)

## 本章目标
这章面向会 Python、懂一点 LangChain、但还没有真正把 AI Agent 当成工程系统来使用的读者。重点不是背术语，而是把“这个功能为什么存在、什么时候该用、怎么安全地用”讲清楚。

## 先用一个高中数学类比
把自动化看成数列中的递推过程。你先给出初值和递推规则，之后系统会在每个时刻自动算出下一项。

## 严谨定义
严格地说，自动化就是一个由“触发条件 + 指令 + 执行环境 + 输出汇总”组成的重复映射。

## 你读完应该抓住什么
- Codex 应用是一个专注的桌面体验，旨在并行处理 Codex 线程，具有内置的工作树支持、自动化和 Git 功能。
- `在项目间多任务处理`：使用一个 Codex 应用窗口在项目间运行任务。为每个代码库添加一个项目，并在需要时切换它们。
- `技能支持`：Codex 应用支持与 CLI 和 IDE 扩展相同的 代理技能。您还可以通过单击侧边栏中的技能查看和探索您的团队在不同项目中创建。

## 分步理解
### 正文
先说直白版：
Codex 应用是一个专注的桌面体验，旨在并行处理 Codex 线程，具有内置的工作树支持、自动化和 Git 功能。
大多数 Codex 应用功能在 macOS 和 Windows 上均可用。以下部分注意平台特定的例外情况。
* * *

把它理解成一个更严谨的过程：
1. Codex 应用是一个专注的桌面体验，旨在并行处理 Codex 线程，具有内置的工作树支持、自动化和 Git 功能。
2. 大多数 Codex 应用功能在 macOS 和 Windows 上均可用。以下部分注意平台特定的例外情况。
3. * * *

### 在项目间多任务处理
先说直白版：
使用一个 Codex 应用窗口在项目间运行任务。为每个代码库添加一个项目，并在需要时切换它们。
如果您使用过 Codex CLI，项目就像是在特定目录中启动一个会话。
如果您在一个单一的代码库中有两个或多个应用或软件包，请将不同的项目分成单独的应用项目，这样 沙盒 中仅包括该项目的文件。

把它理解成一个更严谨的过程：
1. 使用一个 Codex 应用窗口在项目间运行任务。为每个代码库添加一个项目，并在需要时切换它们。
2. 如果您使用过 Codex CLI，项目就像是在特定目录中启动一个会话。
3. 如果您在一个单一的代码库中有两个或多个应用或软件包，请将不同的项目分成单独的应用项目，这样 沙盒 中仅包括该项目的文件。

### 技能支持
先说直白版：
Codex 应用支持与 CLI 和 IDE 扩展相同的 代理技能。您还可以通过单击侧边栏中的技能查看和探索您的团队在不同项目中创建的新技能。

把它理解成一个更严谨的过程：
1. Codex 应用支持与 CLI 和 IDE 扩展相同的 代理技能。您还可以通过单击侧边栏中的技能查看和探索您的团队在不同项目中创建的新技能。

### 自动化
先说直白版：
您还可以将技能与 自动化 结合，执行常规任务，例如评估您在遥测中的错误并提交修复或创建有关最近代码库更改的报告。对于应该保持在一个线程中的持续工作，使用 线程自动化。

把它理解成一个更严谨的过程：
1. 您还可以将技能与 自动化 结合，执行常规任务，例如评估您在遥测中的错误并提交修复或创建有关最近代码库更改的报告。对于应该保持在一个线程中的持续工作，使用 线程自动化。

### 模式
先说直白版：
每个线程在选择的模式下运行。开始线程时，您可以选择：
- **本地**：直接在您当前的项目目录中工作。
- **工作树**：在 Git 工作树中隔离更改。了解更多。
- **云**：在配置的云环境中远程运行。

把它理解成一个更严谨的过程：
1. 每个线程在选择的模式下运行。开始线程时，您可以选择：
2. **本地**：直接在您当前的项目目录中工作。
3. **工作树**：在 Git 工作树中隔离更改。了解更多。
4. **云**：在配置的云环境中远程运行。

## 实战视角
如果你会 Python，可以把这一章里的能力理解成一个可以读文件、调工具、保留状态、按规则执行的程序代理。它和 LangChain 的链、工具、记忆很像，但 Codex 更强调真实工程环境：仓库、命令、审阅、权限、并行任务。

## 给高中水平读者的最后一句话
不要急着一次学完整个体系。把每章都当成“定义 + 例子 + 适用边界”三件事去掌握，AI Agent 就会从模糊概念变成你能实际操作的工程对象。
