# 第36章 环境

> 原始页面：[Cloud environments – Codex web | OpenAI Developers](https://developers.openai.com/codex/cloud/environments)

## 本章目标
这章面向会 Python、懂一点 LangChain、但还没有真正把 AI Agent 当成工程系统来使用的读者。重点不是背术语，而是把“这个功能为什么存在、什么时候该用、怎么安全地用”讲清楚。

## 先用一个高中数学类比
配置像给函数预先设定参数。公式不变，但参数不同，图像和输出会明显不同。

## 严谨定义
严格地说，配置是运行时行为的参数化描述。

## 你读完应该抓住什么
- 使用环境来控制 Codex 在云任务中安装和运行的内容。例如，您可以添加依赖项，安装像 linter 和格式化工具等工具，并设置环境变量。
- `Codex 云任务的运行方式`：提交任务时发生以下情况：
- `默认通用镜像`：Codex 代理在一个名为 `universal` 的默认容器镜像中运行，该镜像预装了常见的语言、包和工具。

## 分步理解
### 正文
先说直白版：
使用环境来控制 Codex 在云任务中安装和运行的内容。例如，您可以添加依赖项，安装像 linter 和格式化工具等工具，并设置环境变量。
在Cedex 设置中配置环境。

把它理解成一个更严谨的过程：
1. 使用环境来控制 Codex 在云任务中安装和运行的内容。例如，您可以添加依赖项，安装像 linter 和格式化工具等工具，并设置环境变量。
2. 在Cedex 设置中配置环境。

### Codex 云任务的运行方式
先说直白版：
提交任务时发生以下情况：
1. Codex 创建一个容器并在选择的分支或提交 SHA 上检出您的 repo。 2. Codex 运行您的设置脚本，缓存容器恢复时还会运行可选的维护脚本。 3. Codex 应用您的互联网访问设置。设置脚本在有互联网访问的情况下运行。代理的互联网访问默认关闭，但您可以在需要时启用有限或不受限制的访问。请参见代理互联网访问。 4. 代理在循环中运行终端命令。它编辑代码，运行检查，并尝试验证其工作。如果您的 repo 包含 `AGENTS.md`，代理将使用它寻找特定项目的 lint 和测试命令。 5. 当代理完成时，它会显示其答案和它更改的任何文件的差异。您可以打开 PR 或提问后续问题。

把它理解成一个更严谨的过程：
1. 提交任务时发生以下情况：
2. 1. Codex 创建一个容器并在选择的分支或提交 SHA 上检出您的 repo。 2. Codex 运行您的设置脚本，缓存容器恢复时还会运行可选的维护脚本。 3. Codex 应用您的互联网访问设置。设置脚本在有互联网访问的情况下运行。代理的互联网访问默认关闭，但您可以在需要时启用有限或不受限制的访问。请参见代理互联网访问。 4. 代理在循环中运行终端命令。它编辑代码，运行检查，并尝试验证其工作。如果您的 repo 包含 `AGENTS.md`，代理将使用它寻找特定项目的 lint 和测试命令。 5. 当代理完成时，它会显示其答案和它更改的任何文件的差异。您可以打开 PR 或提问后续问题。

### 默认通用镜像
先说直白版：
Codex 代理在一个名为 `universal` 的默认容器镜像中运行，该镜像预装了常见的语言、包和工具。
在环境设置中，选择 **设置包版本** 以固定 Python、Node.js 和其他运行时的版本。
有关安装内容的详细信息，请参见 参考 Dockerfile 和可拉取和本地测试的镜像。
虽然 `codex-universal` 为速度和便利性预装了语言，但您也可以使用设置脚本向容器安装额外包。

把它理解成一个更严谨的过程：
1. Codex 代理在一个名为 `universal` 的默认容器镜像中运行，该镜像预装了常见的语言、包和工具。
2. 在环境设置中，选择 **设置包版本** 以固定 Python、Node.js 和其他运行时的版本。
3. 有关安装内容的详细信息，请参见 参考 Dockerfile 和可拉取和本地测试的镜像。
4. 虽然 `codex-universal` 为速度和便利性预装了语言，但您也可以使用设置脚本向容器安装额外包。

### 环境变量和秘密
先说直白版：
**环境变量** 在任务的整个持续时间内设置（包括设置脚本和代理阶段）。
**秘密** 与环境变量类似，但：
- 它们以额外的加密层存储，仅在任务执行时被解密。
- 它们仅对设置脚本可用。出于安全原因，秘密在代理阶段开始前被移除。

把它理解成一个更严谨的过程：
1. **环境变量** 在任务的整个持续时间内设置（包括设置脚本和代理阶段）。
2. **秘密** 与环境变量类似，但：
3. 它们以额外的加密层存储，仅在任务执行时被解密。
4. 它们仅对设置脚本可用。出于安全原因，秘密在代理阶段开始前被移除。

### 自动设置
先说直白版：
对于使用常见包管理器（`npm`、`yarn`、`pnpm`、`pip`、`pipenv` 和 `poetry`）的项目，Codex 可以自动安装依赖项和工具。

把它理解成一个更严谨的过程：
1. 对于使用常见包管理器（`npm`、`yarn`、`pnpm`、`pip`、`pipenv` 和 `poetry`）的项目，Codex 可以自动安装依赖项和工具。

## 实战视角
如果你会 Python，可以把这一章里的能力理解成一个可以读文件、调工具、保留状态、按规则执行的程序代理。它和 LangChain 的链、工具、记忆很像，但 Codex 更强调真实工程环境：仓库、命令、审阅、权限、并行任务。

## 给高中水平读者的最后一句话
不要急着一次学完整个体系。把每章都当成“定义 + 例子 + 适用边界”三件事去掌握，AI Agent 就会从模糊概念变成你能实际操作的工程对象。
