# 第44章 高级配置

> 原始页面：[Advanced Configuration – Codex | OpenAI Developers](https://developers.openai.com/codex/config-advanced)

## 本章目标
这章面向会 Python、懂一点 LangChain、但还没有真正把 AI Agent 当成工程系统来使用的读者。重点不是背术语，而是把“这个功能为什么存在、什么时候该用、怎么安全地用”讲清楚。

## 先用一个高中数学类比
子代理像把一道大题拆成几道小问：主问题不变，但每个小问由更专门的人分别处理。

## 严谨定义
严格地说，子代理是主代理派生出的局部求解器，拥有较小的任务域和更清晰的边界。

## 你读完应该抓住什么
- 在需要更好地控制提供者、策略和集成时使用这些选项。有关快速入门，请参见 配置基础。
- `配置文件`：配置文件允许您保存命名的配置值集，并从 CLI 切换它们。
- `CLI 的一次性覆盖`：除编辑 `~/.codex/config.toml` 外，您还可以从 CLI 覆盖单次运行的配置：

## 分步理解
### 正文
先说直白版：
在需要更好地控制提供者、策略和集成时使用这些选项。有关快速入门，请参见 配置基础。
有关项目指导、可重用能力、自定义斜线命令、子代理工作流和集成的背景信息，请见 定制。有关配置密钥，请参见 配置参考。

把它理解成一个更严谨的过程：
1. 在需要更好地控制提供者、策略和集成时使用这些选项。有关快速入门，请参见 配置基础。
2. 有关项目指导、可重用能力、自定义斜线命令、子代理工作流和集成的背景信息，请见 定制。有关配置密钥，请参见 配置参考。

### 配置文件
先说直白版：
配置文件允许您保存命名的配置值集，并从 CLI 切换它们。
配置文件处于实验状态，可能会在未来的版本中更改或删除。
目前不支持在 Codex IDE 扩展中使用配置文件。
在 `config.toml` 中定义配置文件`[profiles.<name>]`，然后运行 `codex --profile <name>`：

把它理解成一个更严谨的过程：
1. 配置文件允许您保存命名的配置值集，并从 CLI 切换它们。
2. 配置文件处于实验状态，可能会在未来的版本中更改或删除。
3. 目前不支持在 Codex IDE 扩展中使用配置文件。
4. 在 `config.toml` 中定义配置文件`[profiles.<name>]`，然后运行 `codex --profile <name>`：

### CLI 的一次性覆盖
先说直白版：
除编辑 `~/.codex/config.toml` 外，您还可以从 CLI 覆盖单次运行的配置：
- 在存在时优先使用专用标志（例如 `--model`）。
- 当需要覆盖任意键时，请使用 `-c` / `--config`。
示例：

把它理解成一个更严谨的过程：
1. 除编辑 `~/.codex/config.toml` 外，您还可以从 CLI 覆盖单次运行的配置：
2. 在存在时优先使用专用标志（例如 `--model`）。
3. 当需要覆盖任意键时，请使用 `-c` / `--config`。
4. 示例：

### 配置与状态位置
先说直白版：
Codex 将其本地状态存储在 `CODEX_HOME` 下（默认为 `~/.codex`）。
您可能会看到的常见文件：
- `config.toml`（您的本地配置）
- `auth.json`（如果您使用基于文件的凭据存储）或您的操作系统密钥链/密钥环

把它理解成一个更严谨的过程：
1. Codex 将其本地状态存储在 `CODEX_HOME` 下（默认为 `~/.codex`）。
2. 您可能会看到的常见文件：
3. `config.toml`（您的本地配置）
4. `auth.json`（如果您使用基于文件的凭据存储）或您的操作系统密钥链/密钥环

### 项目配置文件 (`.codex/config.toml`)
先说直白版：
除了用户配置外，Codex 还会读取项目范围的覆盖 `.codex/config.toml` 文件，位于您的仓库中。Codex 从项目根目录向您当前的工作目录遍历，并加载它发现的每一个 `.codex/config.toml` 文件。如果多个文件定义了相同的键，则离您工作目录最近的文件获胜。
出于安全原因，仅当项目被信任时，Codex 才会加载项目范围的配置文件。如果项目不被信任，Codex 将忽略项目的 `.codex/` 层，包括 `.codex/config.toml`、项目本地钩子和项目本地规则。用户层和系统层仍然保持独立并继续加载。
项目配置中的相对路径（例如，`model_instructions_file`）相对于包含 `config.toml` 的 `.codex/` 文件夹进行解析。

把它理解成一个更严谨的过程：
1. 除了用户配置外，Codex 还会读取项目范围的覆盖 `.codex/config.toml` 文件，位于您的仓库中。Codex 从项目根目录向您当前的工作目录遍历，并加载它发现的每一个 `.codex/config.toml` 文件。如果多个文件定义了相同的键，则离您工作目录最近的文件获胜。
2. 出于安全原因，仅当项目被信任时，Codex 才会加载项目范围的配置文件。如果项目不被信任，Codex 将忽略项目的 `.codex/` 层，包括 `.codex/config.toml`、项目本地钩子和项目本地规则。用户层和系统层仍然保持独立并继续加载。
3. 项目配置中的相对路径（例如，`model_instructions_file`）相对于包含 `config.toml` 的 `.codex/` 文件夹进行解析。

## 实战视角
如果你会 Python，可以把这一章里的能力理解成一个可以读文件、调工具、保留状态、按规则执行的程序代理。它和 LangChain 的链、工具、记忆很像，但 Codex 更强调真实工程环境：仓库、命令、审阅、权限、并行任务。

## 给高中水平读者的最后一句话
不要急着一次学完整个体系。把每章都当成“定义 + 例子 + 适用边界”三件事去掌握，AI Agent 就会从模糊概念变成你能实际操作的工程对象。
