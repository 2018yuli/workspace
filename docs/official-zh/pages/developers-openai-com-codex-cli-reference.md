---
source: https://developers.openai.com/codex/cli/reference
title: "Command line options – Codex CLI | OpenAI Developers"
translatedTo: zh-CN
translatedAt: 2026-04-27T13:21:18.776Z
model: gpt-4o-mini
---
## 搜索 Codex 文档

搜索文档

### 推荐

worktreesmcpnoninteractivesandbox

主导航

搜索文档

### 推荐

worktreesmcpnoninteractivesandbox

文档 用例

### 入门

- [概述](https://developers.openai.com/codex)
- [快速入门](https://developers.openai.com/codex/quickstart)
- [探索用例](https://developers.openai.com/codex/use-cases)
- [定价](https://developers.openai.com/codex/pricing)
- 概念

  - [提示](https://developers.openai.com/codex/prompting)
  - [定制化](https://developers.openai.com/codex/concepts/customization)
  - [记忆](https://developers.openai.com/codex/memories)

    - [纪事](https://developers.openai.com/codex/memories/chronicle)

  - [沙盒](https://developers.openai.com/codex/concepts/sandboxing)
  - [子代理](https://developers.openai.com/codex/concepts/subagents)
  - [工作流](https://developers.openai.com/codex/workflows)
  - [模型](https://developers.openai.com/codex/models)
  - [网络安全](https://developers.openai.com/codex/concepts/cyber-safety)

### 使用 Codex

- 应用

  - [概述](https://developers.openai.com/codex/app)
  - [功能](https://developers.openai.com/codex/app/features)
  - [设置](https://developers.openai.com/codex/app/settings)
  - [审查](https://developers.openai.com/codex/app/review)
  - [自动化](https://developers.openai.com/codex/app/automations)
  - [工作树](https://developers.openai.com/codex/app/worktrees)
  - [本地环境](https://developers.openai.com/codex/app/local-environments)
  - [应用内浏览器](https://developers.openai.com/codex/app/browser)
  - [计算机使用](https://developers.openai.com/codex/app/computer-use)
  - [命令](https://developers.openai.com/codex/app/commands)
  - [Windows](https://developers.openai.com/codex/app/windows)
  - [故障排除](https://developers.openai.com/codex/app/troubleshooting)

- IDE 扩展

  - [概述](https://developers.openai.com/codex/ide)
  - [功能](https://developers.openai.com/codex/ide/features)
  - [设置](https://developers.openai.com/codex/ide/settings)
  - [IDE 命令](https://developers.openai.com/codex/ide/commands)
  - [斜杠命令](https://developers.openai.com/codex/ide/slash-commands)

- CLI

  - [概述](https://developers.openai.com/codex/cli)
  - [功能](https://developers.openai.com/codex/cli/features)
  - [命令行选项](https://developers.openai.com/codex/cli/reference)
  - [斜杠命令](https://developers.openai.com/codex/cli/slash-commands)

- 网络

  - [概述](https://developers.openai.com/codex/cloud)
  - [环境](https://developers.openai.com/codex/cloud/environments)
  - [互联网访问](https://developers.openai.com/codex/cloud/internet-access)

- 集成

  - [GitHub](https://developers.openai.com/codex/integrations/github)
  - [Slack](https://developers.openai.com/codex/integrations/slack)
  - [Linear](https://developers.openai.com/codex/integrations/linear)

- Codex 安全

  - [概述](https://developers.openai.com/codex/security)
  - [设置](https://developers.openai.com/codex/security/setup)
  - [改善威胁模型](https://developers.openai.com/codex/security/threat-model)
  - [常见问题](https://developers.openai.com/codex/security/faq)

### 配置

- 配置文件

  - [配置基础](https://developers.openai.com/codex/config-basic)
  - [高级配置](https://developers.openai.com/codex/config-advanced)
  - [配置参考](https://developers.openai.com/codex/config-reference)
  - [示例配置](https://developers.openai.com/codex/config-sample)

- [速度](https://developers.openai.com/codex/speed)
- [规则](https://developers.openai.com/codex/rules)
- [钩子](https://developers.openai.com/codex/hooks)
- [AGENTS.md](https://developers.openai.com/codex/guides/agents-md)
- [MCP](https://developers.openai.com/codex/mcp)
- 插件

  - [概述](https://developers.openai.com/codex/plugins)
  - [构建插件](https://developers.openai.com/codex/plugins/build)

- [技能](https://developers.openai.com/codex/skills)
- [子代理](https://developers.openai.com/codex/subagents)

### 管理

- [认证](https://developers.openai.com/codex/auth)
- [代理批准与安全](https://developers.openai.com/codex/agent-approvals-security)
- [远程连接](https://developers.openai.com/codex/remote-connections)
- 企业

  - [管理员设置](https://developers.openai.com/codex/enterprise/admin-setup)
  - [治理](https://developers.openai.com/codex/enterprise/governance)
  - [托管配置](https://developers.openai.com/codex/enterprise/managed-configuration)

- [Windows](https://developers.openai.com/codex/windows)

### 自动化

- [非交互模式](https://developers.openai.com/codex/noninteractive)
- [Codex SDK](https://developers.openai.com/codex/sdk)
- [应用服务器](https://developers.openai.com/codex/app-server)
- [MCP 服务器](https://developers.openai.com/codex/guides/agents-sdk)
- [GitHub Action](https://developers.openai.com/codex/github-action)

### 学习

- [最佳实践](https://developers.openai.com/codex/learn/best-practices)
- [视频](https://developers.openai.com/codex/videos)
- [社区](https://developers.openai.com/community)
- 博客

  - [使用技能加速 OSS 维护](https://developers.openai.com/blog/skills-agents-sdk)
  - [使用 Codex 和 Figma 构建前端 UI](https://developers.openai.com/blog/building-frontend-uis-with-codex-and-figma)
  - [查看全部](https://developers.openai.com/blog/topic/codex)

- 食谱

  - [Codex 提示指南](https://developers.openai.com/cookbook/examples/gpt-5/codex_prompting_guide)
  - [用 Codex 现代化您的代码库](https://developers.openai.com/cookbook/examples/codex/code_modernization)
  - [查看全部](https://developers.openai.com/cookbook/topic/codex)

- [构建 AI 团队](https://developers.openai.com/codex/guides/build-ai-native-engineering-team)

### 发布

- [更新日志](https://developers.openai.com/codex/changelog)
- [功能成熟度](https://developers.openai.com/codex/feature-maturity)
- [开源](https://developers.openai.com/codex/open-source)

[API 仪表板](https://platform.openai.com/login)

复制页面

## 如何阅读此参考

此页面记录了每个文档化的 Codex CLI 命令和标志。使用互动表格按关键字或描述进行搜索。每个部分指示该选项是否稳定或实验性，并指出有风险的组合。

CLI 从 `~/.codex/config.toml` 继承大多数默认值。任何您在命令行中传递的 `-c key=value` 重写在该调用中优先使用。有关更多信息，请参见 [配置基础](https://developers.openai.com/codex/config-basic#configuration-precedence)。

## 全局标志

| 键 | 类型 / 值 | 详细信息 |
| --- | --- | --- |
| `--add-dir` | `path` | 授予其他目录与主工作区相同的写入访问权限。重复以获取多个路径。 |
| `--ask-for-approval, -a` | `untrusted | on-request | never` | 控制 Codex 在运行命令之前何时暂停等待人工批准。 `on-failure` 已弃用；对于交互式运行，优先使用 `on-request`，对于非交互式运行则使用 `never`。 |
| `--cd, -C` | `path` | 在代理开始处理请求之前设置工作目录。 |
| `--config, -c` | `key=value` | 重写配置值。值在可能的情况下解析为 JSON；否则使用字面字符串。 |
| `--dangerously-bypass-approvals-and-sandbox, --yolo` | `boolean` | 在没有批准或沙盒的情况下运行每个命令。仅在外部强化的环境中使用。 |
| `--disable` | `feature` | 强制禁用一个功能标志（对应于 `-c features.<name>=false`）。可重复。 |
| `--enable` | `feature` | 强制启用一个功能标志（对应于 `-c features.<name>=true`）。可重复。 |
| `--full-auto` | `boolean` | 低摩擦本地工作的快捷方式：设置 `--ask-for-approval on-request` 和 `--sandbox workspace-write`。 |
| `--image, -i` | `path[,path...]` | 将一个或多个图像文件附加到初始提示。使用逗号分隔多个路径或重复该标志。 |
| `--model, -m` | `string` | 重写在配置中设置的模型（例如 `gpt-5.4`）。 |
| `--no-alt-screen` | `boolean` | 禁用 TUI 的替代屏幕模式（覆盖 `tui.alternate_screen` 在此运行）。 |
| `--oss` | `boolean` | 使用本地开源模型提供者（相当于 `-c model_provider="oss"`）。验证 Ollama 是否正在运行。 |
| `--profile, -p` | `string` | 要从 `~/.codex/config.toml` 加载的配置配置文件名。 |
| `--remote` | `ws://host:port | wss://host:port` | 将交互式 TUI 连接到远程应用服务器 WebSocket 端点。支持 `codex`、`codex resume` 和 `codex fork`；其他子命令拒绝远程模式。 |
| `--remote-auth-token-env` | `ENV_VAR` | 从此环境变量读取承载令牌，并在使用 `--remote` 连接时发送。需要 `--remote`；仅在 `wss://` URL 或其主机为 `localhost`、`127.0.0.1` 或 `::1` 的 `ws://` URL 上发送令牌。 |
| `--sandbox, -s` | `read-only | workspace-write | danger-full-access` | 选择模型生成的 shell 命令的沙盒策略。 |
| `--search` | `boolean` | 启用实时网络搜索（将 `web_search` 设置为 `"live"`，而不是默认的 `"cached"`）。 |
| `PROMPT` | `string` | 启动会话的可选文本指令。省略以在没有预填消息的情况下启动 TUI。 |

键

`--add-dir`

类型 / 值

`path`

详细信息

授予其他目录与主工作区相同的写入访问权限。重复以获取多个路径。

键

`--ask-for-approval, -a`

类型 / 值

`untrusted | on-request | never`

详细信息

控制 Codex 在运行命令之前何时暂停等待人工批准。 `on-failure` 已弃用；对于交互式运行，优先使用 `on-request`，对于非交互式运行则使用 `never`。

键

`--cd, -C`

类型 / 值

`path`

详细信息

在代理开始处理请求之前设置工作目录。

键

`--config, -c`

类型 / 值

`key=value`

详细信息

重写配置值。值在可能的情况下解析为 JSON；否则使用字面字符串。

键

`--dangerously-bypass-approvals-and-sandbox, --yolo`

类型 / 值

`boolean`

详细信息

在没有批准或沙盒的情况下运行每个命令。仅在外部强化的环境中使用。

键

`--disable`

类型 / 值

`feature`

详细信息

强制禁用一个功能标志（对应于 `-c features.<name>=false`）。可重复。

键

`--enable`

类型 / 值

`feature`

详细信息

强制启用一个功能标志（对应于 `-c features.<name>=true`）。可重复。

键

`--full-auto`

类型 / 值

`boolean`

详细信息

低摩擦本地工作的快捷方式：设置 `--ask-for-approval on-request` 和 `--sandbox workspace-write`。

键

`--image, -i`

类型 / 值

`path[,path...]`

详细信息

将一个或多个图像文件附加到初始提示。使用逗号分隔多个路径或重复该标志。

键

`--model, -m`

类型 / 值

`string`

详细信息

重写在配置中设置的模型（例如 `gpt-5.4`）。

键

`--no-alt-screen`

类型 / 值

`boolean`

详细信息

禁用 TUI 的替代屏幕模式（覆盖 `tui.alternate_screen` 在此运行）。

键

`--oss`

类型 / 值

`boolean`

详细信息

使用本地开源模型提供者（相当于 `-c model_provider="oss"`）。验证 Ollama 是否正在运行。

键

`--profile, -p`

类型 / 值

`string`

详细信息

要从 `~/.codex/config.toml` 加载的配置配置文件名。

键

`--remote`

类型 / 值

`ws://host:port | wss://host:port`

详细信息

将交互式 TUI 连接到远程应用服务器 WebSocket 端点。支持 `codex`、`codex resume` 和 `codex fork`；其他子命令拒绝远程模式。

键

`--remote-auth-token-env`

类型 / 值

`ENV_VAR`

详细信息

从此环境变量读取承载令牌，并在使用 `--remote` 连接时发送。需要 `--remote`；仅在 `wss://` URL 或其主机为 `localhost`、`127.0.0.1` 或 `::1` 的 `ws://` URL 上发送令牌。

键

`--sandbox, -s`

类型 / 值

`read-only | workspace-write | danger-full-access`

详细信息

选择模型生成的 shell 命令的沙盒策略。

键

`--search`

类型 / 值

`boolean`

详细信息

启用实时网络搜索（将 `web_search` 设置为 `"live"`，而不是默认的 `"cached"`）。

键

`PROMPT`

类型 / 值

`string`

详细信息

启动会话的可选文本指令。省略以在没有预填消息的情况下启动 TUI。

展开以查看所有

这些选项适用于基本的 `codex` 命令，并传播到每个子命令，除非下面的某个部分另有说明。
当您运行子命令时，请将全局标志放在其后（例如，`codex exec --oss ...`），以便 Codex 按照预期应用它们。

## 命令概述

成熟度列使用功能成熟度标签，例如实验性、Beta 和稳定。有关如何解释这些标签的信息，请参见 [功能成熟度](https://developers.openai.com/codex/feature-maturity)。

| 键 | 成熟度 | 详细信息 |
| --- | --- | --- |
| [`codex`](https://developers.openai.com/codex/cli/reference#codex-interactive) | 稳定 | 启动终端用户界面。接受上述全局标志以及可选提示或图像附加。 |
| [`codex app`](https://developers.openai.com/codex/cli/reference#codex-app) | 稳定 | 在 macOS 或 Windows 上启动 Codex 桌面应用。 在 macOS 上，Codex 可以打开工作区路径；在 Windows 上，Codex 打印要打开的路径。 |
| [`codex app-server`](https://developers.openai.com/codex/cli/reference#codex-app-server) | 实验性 | 启动用于本地开发或调试的 Codex 应用服务器。 |
| [`codex apply`](https://developers.openai.com/codex/cli/reference#codex-apply) | 稳定 | 将 Codex Cloud 任务生成的最新差异应用到您的本地工作树。别名：`codex a`。 |
| [`codex cloud`](https://developers.openai.com/codex/cli/reference#codex-cloud) | 实验性 | 从终端浏览或执行 Codex Cloud 任务，而不打开 TUI。别名：`codex cloud-tasks`。 |
| [`codex completion`](https://developers.openai.com/codex/cli/reference#codex-completion) | 稳定 | 为 Bash、Zsh、Fish 或 PowerShell 生成 shell 完成脚本。 |
| [`codex debug app-server send-message-v2`](https://developers.openai.com/codex/cli/reference#codex-debug-app-server-send-message-v2) | 实验性 | 通过内置测试客户端发送单个 V2 消息以调试应用服务器。 |
| [`codex exec`](https://developers.openai.com/codex/cli/reference#codex-exec) | 稳定 | 以非交互方式运行 Codex。别名：`codex e`。将结果流式传输到 stdout 或 JSONL，并可选择恢复先前的会话。 |
| [`codex execpolicy`](https://developers.openai.com/codex/cli/reference#codex-execpolicy) | 实验性 | 评估 execpolicy 规则文件，并查看命令是否被允许、提示或阻止。 |
| [`codex features`](https://developers.openai.com/codex/cli/reference#codex-features) | 稳定 | 列出功能标志，并在 `config.toml` 中持久化启用或禁用它们。 |
| [`codex fork`](https://developers.openai.com/codex/cli/reference#codex-fork) | 稳定 | 将先前的交互会话分叉到新线程，保留原始记录。 |
| [`codex login`](https://developers.openai.com/codex/cli/reference#codex-login) | 稳定 | 使用 ChatGPT OAuth、设备认证或通过 stdin 管道传递的 API 密钥认证 Codex。 |
| [`codex logout`](https://developers.openai.com/codex/cli/reference#codex-logout) | 稳定 | 移除存储的认证凭据。 |
| [`codex mcp`](https://developers.openai.com/codex/cli/reference#codex-mcp) | 实验性 | 管理模型上下文协议服务器（列表、添加、删除、认证）。 |
| [`codex mcp-server`](https://developers.openai.com/codex/cli/reference#codex-mcp-server) | 实验性 | 通过 stdio 运行 Codex 本身作为 MCP 服务器。当另一个代理使用 Codex 时非常有用。 |
| [`codex plugin marketplace`](https://developers.openai.com/codex/cli/reference#codex-plugin-marketplace) | 实验性 | 从 Git 或本地源添加、升级或删除插件市场。 |
| [`codex resume`](https://developers.openai.com/codex/cli/reference#codex-resume) | 稳定 | 通过 ID 继续先前的交互会话，或恢复最近的对话。 |
| [`codex sandbox`](https://developers.openai.com/codex/cli/reference#codex-sandbox) | 实验性 | 在 Codex 提供的 macOS seatbelt 或 Linux bubblewrap 沙盒中运行任意命令。 |

键

[`codex`](https://developers.openai.com/codex/cli/reference#codex-interactive)

成熟度

稳定

详细信息

启动终端用户界面。接受上述全局标志以及可选提示或图像附加。

键

[`codex app`](https://developers.openai.com/codex/cli/reference#codex-app)

成熟度

稳定

详细信息

在 macOS 或 Windows 上启动 Codex 桌面应用。 在 macOS 上，Codex 可以打开工作区路径；在 Windows 上，Codex 打印要打开的路径。

键

[`codex app-server`](https://developers.openai.com/codex/cli/reference#codex-app-server)

成熟度

实验性

详细信息

启动用于本地开发或调试的 Codex 应用服务器。

键

[`codex apply`](https://developers.openai.com/codex/cli/reference#codex-apply)

成熟度

稳定

详细信息

将 Codex Cloud 任务生成的最新差异应用到您的本地工作树。别名：`codex a`。

键

[`codex cloud`](https://developers.openai.com/codex/cli/reference#codex-cloud)

成熟度

实验性

详细信息

从终端浏览或执行 Codex Cloud 任务，而不打开 TUI。别名：`codex cloud-tasks`。

键

[`codex completion`](https://developers.openai.com/codex/cli/reference#codex-completion)

成熟度

稳定

详细信息

为 Bash、Zsh、Fish 或 PowerShell 生成 shell 完成脚本。

键

[`codex debug app-server send-message-v2`](https://developers.openai.com/codex/cli/reference#codex-debug-app-server-send-message-v2)

成熟度

实验性

详细信息

通过内置测试客户端发送单个 V2 消息以调试应用服务器。

键

[`codex exec`](https://developers.openai.com/codex/cli/reference#codex-exec)

成熟度

稳定

详细信息

以非交互方式运行 Codex。别名：`codex e`。将结果流式传输到 stdout 或 JSONL，并可选择恢复先前的会话。

键

[`codex execpolicy`](https://developers.openai.com/codex/cli/reference#codex-execpolicy)

成熟度

实验性

详细信息

评估 execpolicy 规则文件，并查看命令是否被允许、提示或阻止。

键

[`codex features`](https://developers.openai.com/codex/cli/reference#codex-features)

成熟度

稳定

详细信息

列出功能标志，并在 `config.toml` 中持久化启用或禁用它们。

键

[`codex fork`](https://developers.openai.com/codex/cli/reference#codex-fork)

成熟度

稳定

详细信息

将先前的交互会话分叉到新线程，保留原始记录。

键

[`codex login`](https://developers.openai.com/codex/cli/reference#codex-login)

成熟度

稳定

详细信息

使用 ChatGPT OAuth、设备认证或通过 stdin 管道传递的 API 密钥认证 Codex。

键

[`codex logout`](https://developers.openai.com/codex/cli/reference#codex-logout)

成熟度

稳定

详细信息

移除存储的认证凭据。

键

[`codex mcp`](https://developers.openai.com/codex/cli/reference#codex-mcp)

成熟度

实验性

详细信息

管理模型上下文协议服务器（列表、添加、删除、认证）。

键

[`codex mcp-server`](https://developers.openai.com/codex/cli/reference#codex-mcp-server)

成熟度

实验性

详细信息

通过 stdio 运行 Codex 本身作为 MCP 服务器。当另一个代理使用 Codex 时非常有用。

键

[`codex plugin marketplace`](https://developers.openai.com/codex/cli/reference#codex-plugin-marketplace)

成熟度

实验性

详细信息

从 Git 或本地源添加、升级或删除插件市场。

键

[`codex resume`](https://developers.openai.com/codex/cli/reference#codex-resume)

成熟度

稳定

详细信息

通过 ID 继续先前的交互会话，或恢复最近的对话。

键

[`codex sandbox`](https://developers.openai.com/codex/cli/reference#codex-sandbox)

成熟度

实验性

详细信息

在 Codex 提供的 macOS seatbelt 或 Linux bubblewrap 沙盒中运行任意命令。

展开以查看全部

## 命令详细信息

### `codex`（交互式）

运行 `codex` 而不带子命令启动交互式终端用户界面（TUI）。代理接受上述全局标志以及图像附件。网络搜索默认为缓存模式；使用 `--search` 切换到实时浏览，并使用 `--full-auto` 让 Codex 在没有提示的情况下运行大部分命令。

使用 `--remote ws://host:port` 或 `--remote wss://host:port` 将 TUI 连接到使用 `codex app-server --listen ws://IP:PORT` 启动的应用服务器。当前服务器需要承载令牌进行 WebSocket 身份验证时，请添加 `--remote-auth-token-env <ENV_VAR>`。有关设置示例和身份验证指导，请参见 [Codex CLI 功能](https://developers.openai.com/codex/cli/features#connect-the-tui-to-a-remote-app-server)。

### `codex app-server`

在本地启动 Codex 应用服务器。这主要用于开发和调试，可能会更改而不另行通知。

| 键 | 类型 / 值 | 详细信息 |
| --- | --- | --- |
| `--listen` | `stdio:// | ws://IP:PORT` | 传输监听器 URL。使用 `ws://IP:PORT` 为远程客户端公开 WebSocket 端点。 |
| `--ws-audience` | `string` | 签名的承载令牌预期的 `aud` 声明。需要 `--ws-auth signed-bearer-token`。 |
| `--ws-auth` | `capability-token | signed-bearer-token` | 应用服务器 WebSocket 客户端的身份验证模式。如果省略，则禁用 WebSocket 身份验证；本地外部监听器在启动时发出警告。 |
| `--ws-issuer` | `string` | 签名的承载令牌预期的 `iss` 声明。需要 `--ws-auth signed-bearer-token`。 |
| `--ws-max-clock-skew-seconds` | `number` | 在验证签名的承载令牌 `exp` 和 `nbf` 声明时的时钟偏差容忍度。需要 `--ws-auth signed-bearer-token`。 |
| `--ws-shared-secret-file` | `absolute path` | 包含用于验证签名 JWT 承载令牌的 HMAC 共享秘密的文件。与 `--ws-auth signed-bearer-token` 一起使用。 |
| `--ws-token-file` | `absolute path` | 包含共享能力令牌的文件。与 `--ws-auth capability-token` 一起使用。 |

键

`--listen`

类型 / 值

`stdio:// | ws://IP:PORT`

详细信息

传输监听器 URL。使用 `ws://IP:PORT` 为远程客户端公开 WebSocket 端点。

键

`--ws-audience`

类型 / 值

`string`

详细信息

签名的承载令牌预期的 `aud` 声明。需要 `--ws-auth signed-bearer-token`。

键

`--ws-auth`

类型 / 值

`capability-token | signed-bearer-token`

详细信息

应用服务器 WebSocket 客户端的身份验证模式。如果省略，则禁用 WebSocket 身份验证；本地外部监听器在启动时发出警告。

键

`--ws-issuer`

类型 / 值

`string`

详细信息

签名的承载令牌预期的 `iss` 声明。需要 `--ws-auth signed-bearer-token`。

键

`--ws-max-clock-skew-seconds`

类型 / 值

`number`

详细信息

在验证签名的承载令牌 `exp` 和 `nbf` 声明时的时钟偏差容忍度。需要 `--ws-auth signed-bearer-token`。

键

`--ws-shared-secret-file`

类型 / 值

`absolute path`

详细信息

包含用于验证签名 JWT 承载令牌的 HMAC 共享秘密的文件。与 `--ws-auth signed-bearer-token` 一起使用。

键

`--ws-token-file`

类型 / 值

`absolute path`

详细信息

包含共享能力令牌的文件。与 `--ws-auth capability-token` 一起使用。

`codex app-server --listen stdio://` 保持默认的 JSONL-over-stdio 行为。 `--listen ws://IP:PORT` 启用应用服务器客户端的 WebSocket 传输。服务器接受 `ws://` 监听 URL；当客户端通过 `wss://` 连接时，请使用 TLS 终止或安全代理。如果您为客户端绑定生成架构，请添加 `--experimental` 以包含受限字段和方法。

### `codex app`

从终端在 macOS 或 Windows 上启动 Codex 桌面应用。在 macOS 上，Codex 可以打开特定的工作区路径；在 Windows 上，Codex 打印要打开的路径。

| 键 | 类型 / 值 | 详细信息 |
| --- | --- | --- |
| `--download-url` | `url` | 用于安装期间的 Codex 桌面安装程序 URL 的高级覆盖。 |
| `PATH` | `path` | Codex 桌面工作区路径。在 macOS 上，Codex 打开此路径；在 Windows 上，Codex 打印该路径。 |

键

`--download-url`

类型 / 值

`url`

详细信息

用于安装期间的 Codex 桌面安装程序 URL 的高级覆盖。

键

`PATH`

类型 / 值

`path`

详细信息

Codex 桌面工作区路径。在 macOS 上，Codex 打开此路径；在 Windows 上，Codex 打印该路径。

`codex app` 打开已安装的 Codex 桌面应用，或者在缺失该应用时启动安装程序。在 macOS 上，Codex 打开提供的工作区路径；在 Windows 上，它在安装后打印要打开的路径。

### `codex debug app-server send-message-v2`

通过应用服务器的 V2 线程/回合流程发送一条消息，使用内置的应用服务器测试客户端。

| 键 | 类型 / 值 | 详细信息 |
| --- | --- | --- |
| `USER_MESSAGE` | `string` | 发送给应用服务器的消息文本，通过内置 V2 测试客户端流程。 |

键

`USER_MESSAGE`

类型 / 值

`string`

详细信息

发送给应用服务器的消息文本，通过内置 V2 测试客户端流程。

此调试流程以 `experimentalApi: true` 初始化，启动一个线程，发送一个回合，并流式传输服务器通知。使用它在本地重现和检查应用服务器协议行为。

### `codex apply`

将 Codex Cloud 任务的最新差异应用到您的本地存储库。您必须进行身份验证并具有访问该任务的权限。

| 键 | 类型 / 值 | 详细信息 |
| --- | --- | --- |
| `TASK_ID` | `string` | 应用差异的 Codex Cloud 任务的标识符。 |

键

`TASK_ID`

类型 / 值

`string`

详细信息

应用差异的 Codex Cloud 任务的标识符。

Codex 在输出中打印打补丁的文件，如果 `git apply` 失败（例如，由于冲突），则退出时返回非零值。

### `codex cloud`

从终端与 Codex Cloud 任务交互。默认命令打开交互式选择器；`codex cloud exec` 直接提交任务，`codex cloud list` 返回最近的任务以供脚本或快速检查。

| 键 | 类型 / 值 | 详细信息 |
| --- | --- | --- |
| `--attempts` | `1-4` | Codex Cloud 应运行的助手尝试次数（最佳结果）。 |
| `--env` | `ENV_ID` | 目标 Codex Cloud 环境标识符（必需）。使用 `codex cloud` 列出选项。 |
| `QUERY` | `string` | 任务提示。如果省略，则 Codex 会交互式提示详细信息。 |

键

`--attempts`

类型 / 值

`1-4`

详细信息

Codex Cloud 应运行的助手尝试次数（最佳结果）。

键

`--env`

类型 / 值

`ENV_ID`

详细信息

目标 Codex Cloud 环境标识符（必需）。使用 `codex cloud` 列出选项。

键

`QUERY`

类型 / 值

`string`

详细信息

任务提示。如果省略，则 Codex 会交互式提示详细信息。

身份验证遵循与主 CLI 相同的凭据。如果任务提交失败，Codex 将退出非零。

#### `codex cloud list`

列出最近的云任务，具有可选过滤和分页。

| 键 | 类型 / 值 | 详细信息 |
| --- | --- | --- |
| `--cursor` | `string` | 先前请求返回的分页游标。 |
| `--env` | `ENV_ID` | 按环境标识符过滤任务。 |
| `--json` | `boolean` | 输出机器可读的 JSON，而不是纯文本。 |
| `--limit` | `1-20` | 返回的最大任务数量。 |

键

`--cursor`

类型 / 值

`string`

详细信息

先前请求返回的分页游标。

键

`--env`

类型 / 值

`ENV_ID`

详细信息

按环境标识符过滤任务。

键

`--json`

类型 / 值

`boolean`

详细信息

输出机器可读的 JSON，而不是纯文本。

键

`--limit`

类型 / 值

`1-20`

详细信息

返回的最大任务数量。

纯文本输出打印任务 URL 及其状态详细信息。使用 `--json` 进行自动化。 JSON 有效载荷包含 `tasks` 数组和可选的 `cursor` 值。每个任务包括 `id`、`url`、`title`、`status`、`updated_at`、`environment_id`、`environment_label`、`summary`、`is_review` 和 `attempt_total`。

### `codex completion`

生成 shell 完成脚本并将输出重定向到适当的位置，例如 `codex completion zsh > "${fpath[1]}/_codex"`。

| 键 | 类型 / 值 | 详细信息 |
| --- | --- | --- |
| `SHELL` | `bash | zsh | fish | power-shell | elvish` | 生成完成的 shell。输出打印到 stdout。 |

键

`SHELL`

类型 / 值

`bash | zsh | fish | power-shell | elvish`

详细信息

生成完成的 shell。输出打印到 stdout。

### `codex features`

管理存储在 `~/.codex/config.toml` 中的功能标志。 `enable` 和 `disable` 命令持久化更改，以便在将来的会话中应用。当您使用 `--profile` 启动时，Codex 会将更改写入该配置文件，而不是根配置。

| 键 | 类型 / 值 | 详细信息 |
| --- | --- | --- |
| `禁用子命令` | `codex features disable <feature>` | 在 `config.toml` 中持久禁用功能标志。遵循提供的活跃 `--profile`。 |
| `启用子命令` | `codex features enable <feature>` | 在 `config.toml` 中持久启用功能标志。遵循提供的活跃 `--profile`。 |
| `列表子命令` | `codex features list` | 显示已知功能标志、其成熟阶段和有效状态。 |

键

`禁用子命令`

类型 / 值

`codex features disable <feature>`

详细信息

在 `config.toml` 中持久禁用功能标志。遵循提供的活跃 `--profile`。

键

`启用子命令`

类型 / 值

`codex features enable <feature>`

详细信息

在 `config.toml` 中持久启用功能标志。遵循提供的活跃 `--profile`。

键

`列表子命令`

类型 / 值

`codex features list`

详细信息

显示已知功能标志、其成熟阶段和有效状态。

### `codex exec`

使用 `codex exec`（或缩写 `codex e`）进行不需要人工互动的脚本或 CI 风格的运行。

| 键 | 类型 / 值 | 详细信息 |
| --- | --- | --- |
| `--cd, -C` | `path` | 在执行任务之前设置工作区根。 |
| `--color` | `always | never | auto` | 控制 stdout 中的 ANSI 颜色。 |
| `--dangerously-bypass-approvals-and-sandbox, --yolo` | `boolean` | 绕过批准提示和沙盒。危险—仅在隔离运行器内使用。 |
| `--ephemeral` | `boolean` | 运行时不会将会话推出文件持久化到磁盘。 |
| `--full-auto` | `boolean` | 应用低摩擦自动化预设（`workspace-write` 沙盒和 `on-request` 批准）。 |
| `--image, -i` | `path[,path...]` | 将图像附加到第一条消息。可重复；支持逗号分隔列表。 |
| `--json, --experimental-json` | `boolean` | 打印换行分隔的 JSON 事件，而不是格式化文本。 |
| `--model, -m` | `string` | 重写此运行的配置模型。 |
| `--oss` | `boolean` | 使用本地开源提供者（需要一个运行中的 Ollama 实例）。 |
| `--output-last-message, -o` | `path` | 将助手的最后一条消息写入文件。对下游脚本很有用。 |
| `--output-schema` | `path` | 描述预期最终响应形状的 JSON Schema 文件。Codex 会对工具输出进行验证。 |
| `--profile, -p` | `string` | 从 config.toml 中选择定义的配置配置文件。 |
| `--sandbox, -s` | `read-only | workspace-write | danger-full-access` | 模型生成命令的沙盒策略。默认使用配置。 |
| `--skip-git-repo-check` | `boolean` | 允许在 Git 存储库外部运行（适用于一次性目录）。 |
| `-c, --config` | `key=value` | 针对非交互式运行的内联配置重写（可重复）。 |
| `PROMPT` | `string | - (read stdin)` | 任务的初始指令。使用 `-` 从 stdin 管道提示。 |
| `恢复子命令` | `codex exec resume [SESSION_ID]` | 按 ID 恢复 exec 会话，或添加 `--last` 继续当前工作目录中的最近会话。添加 `--all` 考虑任何目录中的会话。接受可选的后续提示。 |

键

`--cd, -C`

类型 / 值

`path`

详细信息

在执行任务之前设置工作区根。

键

`--color`

类型 / 值

`always | never | auto`

详细信息

控制 stdout 中的 ANSI 颜色。

键

`--dangerously-bypass-approvals-and-sandbox, --yolo`

类型 / 值

`boolean`

详细信息

绕过批准提示和沙盒。危险—仅在隔离运行器内使用。

键

`--ephemeral`

类型 / 值

`boolean`

详细信息

运行时不会将会话推出文件持久化到磁盘。

键

`--full-auto`

类型 / 值

`boolean`

详细信息

应用低摩擦自动化预设（`workspace-write` 沙盒和 `on-request` 批准）。

键

`--image, -i`

类型 / 值

`path[,path...]`

详细信息

将图像附加到第一条消息。可重复；支持逗号分隔列表。

键

`--json, --experimental-json`

类型 / 值

`boolean`

详细信息

打印换行分隔的 JSON 事件，而不是格式化文本。

键

`--model, -m`

类型 / 值

`string`

详细信息

重写此运行的配置模型。

键

`--oss`

类型 / 值

`boolean`

详细信息

使用本地开源提供者（需要一个运行中的 Ollama 实例）。

键

`--output-last-message, -o`

类型 / 值

`path`

详细信息

将助手的最后一条消息写入文件。对下游脚本很有用。

键

`--output-schema`

类型 / 值

`path`

详细信息

描述预期最终响应形状的 JSON Schema 文件。Codex 会对工具输出进行验证。

键

`--profile, -p`

类型 / 值

`string`

详细信息

从 config.toml 中选择定义的配置配置文件。

键

`--sandbox, -s`

类型 / 值

`read-only | workspace-write | danger-full-access`

详细信息

模型生成命令的沙盒策略。默认使用配置。

键

`--skip-git-repo-check`

类型 / 值

`boolean`

详细信息

允许在 Git 存储库外部运行（适用于一次性目录）。

键

`-c, --config`

类型 / 值

`key=value`

详细信息

针对非交互式运行的内联配置重写（可重复）。

键

`PROMPT`

类型 / 值

`string | - (read stdin)`

详细信息

任务的初始指令。使用 `-` 从 stdin 管道提示。

键

`恢复子命令`

类型 / 值

`codex exec resume [SESSION_ID]`

详细信息

按 ID 恢复 exec 会话，或添加 `--last` 继续当前工作目录中的最近会话。添加 `--all` 考虑任何目录中的会话。接受可选的后续提示。

展开以查看全部

Codex 默认输出格式化输出。添加 `--json` 以接收换行分隔的 JSON 事件（每个状态更改一个）。可选的 `resume` 子命令允许您继续非交互式任务。使用 `--last` 从当前工作目录选择最近的会话，或添加 `--all` 在所有会话中搜索：

| 键 | 类型 / 值 | 详细信息 |
| --- | --- | --- |
| `--all` | `boolean` | 选择最近会话时包括当前工作目录之外的会话。 |
| `--image, -i` | `path[,path...]` | 将一个或多个图像附加到后续提示。使用逗号分隔多个路径或重复该标志。 |
| `--last` | `boolean` | 从当前工作目录恢复最近的会话。 |
| `PROMPT` | `string | - (read stdin)` | 可选的后续指令，立即在恢复后发送。 |
| `SESSION_ID` | `uuid` | 恢复指定的会话。省略并使用 `--last` 继续最近的会话。 |

键

`--all`

类型 / 值

`boolean`

详细信息

选择最近会话时包括当前工作目录之外的会话。

键

`--image, -i`

类型 / 值

`path[,path...]`

详细信息

将一个或多个图像附加到后续提示。使用逗号分隔多个路径或重复该标志。

键

`--last`

类型 / 值

`boolean`

详细信息

从当前工作目录恢复最近的会话。

键

`PROMPT`

类型 / 值

`string | - (read stdin)`

详细信息

可选的后续指令，立即在恢复后发送。

键

`SESSION_ID`

类型 / 值

`uuid`

详细信息

恢复指定的会话。省略并使用 `--last` 继续最近的会话。

### `codex execpolicy`

在您保存之前检查 `execpolicy` 规则文件。 `codex execpolicy check` 接受一个或多个 `--rules` 标志（例如，`~/.codex/rules` 下的文件），并发出 JSON 显示最严格的决定和任何匹配的规则。添加 `--pretty` 以格式化输出。 `execpolicy` 命令目前处于预览状态。

| 键 | 类型 / 值 | 详细信息 |
| --- | --- | --- |
| `--pretty` | `boolean` | 以美观的格式打印 JSON 结果。 |
| `--rules, -r` | `path (可重复)` | 要评估的 execpolicy 规则文件的路径。提供多个标志以跨文件组合规则。 |
| `COMMAND...` | `var-args` | 要根据指定策略检查的命令。 |

键

`--pretty`

类型 / 值

`boolean`

详细信息

以美观的格式打印 JSON 结果。

键

`--rules, -r`

类型 / 值

`path (可重复)`

详细信息

要评估的 execpolicy 规则文件的路径。提供多个标志以跨文件组合规则。

键

`COMMAND...`

类型 / 值

`var-args`

详细信息

要根据指定策略检查的命令。

### `codex login`

使用 ChatGPT 账户或 API 密钥对 CLI 进行身份验证。没有标志时，Codex 会打开一个浏览器进行 ChatGPT OAuth 流程。

| 键 | 类型 / 值 | 详细信息 |
| --- | --- | --- |
| `--device-auth` | `boolean` | 使用 OAuth 设备码流而不是启动浏览器窗口。 |
| `--with-api-key` | `boolean` | 从 stdin 读取 API 密钥（例如 `printenv OPENAI_API_KEY | codex login --with-api-key`）。 |
| `status 子命令` | `codex login status` | 打印当前活跃的身份验证模式，并在已登录时以 0 退出。 |

键

`--device-auth`

类型 / 值

`boolean`

详细信息

使用 OAuth 设备码流而不是启动浏览器窗口。

键

`--with-api-key`

类型 / 值

`boolean`

详细信息

从 stdin 读取 API 密钥（例如 `printenv OPENAI_API_KEY | codex login --with-api-key`）。

键

`status 子命令`

类型 / 值

`codex login status`

详细信息

打印当前活跃的身份验证模式，并在已登录时以 0 退出。

`codex login status` 在凭证存在时以 `0` 退出，这在自动化脚本中很有帮助。

### `codex logout`

移除 API 密钥和 ChatGPT 身份验证的已保存凭据。此命令没有标志。

### `codex mcp`

管理存储在 `~/.codex/config.toml` 中的模型上下文协议服务器条目。

| 键 | 类型 / 值 | 详细信息 |
| --- | --- | --- |
| `add <name>` | `-- <command...> | --url <value>` | 使用 stdio 启动命令或可流式传输的 HTTP URL 注册服务器。支持 `--env KEY=VALUE` 用于 stdio 传输。 |
| `get <name>` | `--json` | 显示特定服务器配置。`--json` 打印原始配置条目。 |
| `list` | `--json` | 列出配置的 MCP 服务器。添加 `--json` 以获取机器可读的输出。 |
| `login <name>` | `--scopes scope1,scope2` | 为可流式传输的 HTTP 服务器（仅限支持 OAuth 的服务器）启动 OAuth 登录。 |
| `logout <name>` |  | 删除可流式传输的 HTTP 服务器已存储的 OAuth 凭据。 |
| `remove <name>` |  | 删除存储的 MCP 服务器定义。 |

键

`add <name>`

类型 / 值

`-- <command...> | --url <value>`

详细信息

使用 stdio 启动命令或可流式传输的 HTTP URL 注册服务器。支持 `--env KEY=VALUE` 用于 stdio 传输。

键

`get <name>`

类型 / 值

`--json`

详细信息

显示特定服务器配置。`--json` 打印原始配置条目。

键

`list`

类型 / 值

`--json`

详细信息

列出配置的 MCP 服务器。添加 `--json` 以获取机器可读的输出。

键

`login <name>`

类型 / 值

`--scopes scope1,scope2`

详细信息

为可流式传输的 HTTP 服务器（仅限支持 OAuth 的服务器）启动 OAuth 登录。

键

`logout <name>`

详细信息

删除可流式传输的 HTTP 服务器已存储的 OAuth 凭据。

键

`remove <name>`

详细信息

删除存储的 MCP 服务器定义。

`add` 子命令支持 stdio 和可流式传输的 HTTP 传输：

| 键 | 类型 / 值 | 详细信息 |
| --- | --- | --- |
| `--bearer-token-env-var` | `ENV_VAR` | 连接到可流式传输的 HTTP 服务器时，将值作为承载令牌发送的环境变量。 |
| `--env KEY=VALUE` | `repeatable` | 在启动 stdio 服务器时应用的环境变量赋值。 |
| `--url` | `https://…` | 注册一个可流式传输的 HTTP 服务器，而不是 stdio。与 `COMMAND...` 互斥。 |
| `COMMAND...` | `stdio transport` | 启动 MCP 服务器的可执行文件和参数。提供在 `--` 之后。 |

键

`--bearer-token-env-var`

类型 / 值

`ENV_VAR`

详细信息

连接到可流式传输的 HTTP 服务器时，将值作为承载令牌发送的环境变量。

键

`--env KEY=VALUE`

类型 / 值

`repeatable`

详细信息

在启动 stdio 服务器时应用的环境变量赋值。

键

`--url`

类型 / 值

`https://…`

详细信息

注册一个可流式传输的 HTTP 服务器，而不是 stdio。与 `COMMAND...` 互斥。

键

`COMMAND...`

类型 / 值

`stdio transport`

详细信息

启动 MCP 服务器的可执行文件和参数。提供在 `--` 之后。

OAuth 操作（`login`、`logout`）仅适用于可流式传输的 HTTP 服务器（并且仅在服务器支持 OAuth 时）。

### `codex plugin marketplace`

管理 Codex 可以浏览和安装的插件市场源。

| 键 | 类型 / 值 | 详细信息 |
| --- | --- | --- |
| `add <source>` | `[--ref REF] [--sparse PATH]` | 从 GitHub 简写、Git URL、SSH URL 或本地市场根目录安装插件市场。仅支持 `--sparse` 用于 Git 源，且可以重复。 |
| `remove <marketplace-name>` |  | 删除配置的插件市场。 |
| `upgrade [marketplace-name]` |  | 刷新一个配置的 Git 市场，或在未提供名称时刷新所有配置的 Git 市场。 |

键

`add <source>`

类型 / 值

`[--ref REF] [--sparse PATH]`

详细信息

从 GitHub 简写、Git URL、SSH URL 或本地市场根目录安装插件市场。仅支持 `--sparse` 用于 Git 源，且可以重复。

键

`remove <marketplace-name>`

详细信息

删除配置的插件市场。

键

`upgrade [marketplace-name]`

详细信息

刷新一个配置的 Git 市场，或在未提供名称时刷新所有配置的 Git 市场。

`codex plugin marketplace add` 接受 GitHub 简写，例如 `owner/repo` 或 `owner/repo@ref`、HTTP 或 HTTPS Git URL、SSH Git URL，以及本地市场根目录。使用 `--ref` 钉住一个 Git ref，重复 `--sparse PATH` 以使用稀疏检出 Git 支持的市场库。

### `codex mcp-server`

通过 stdio 运行 Codex 作为 MCP 服务器，以便其他工具可以连接。此命令继承全局配置重写，并在下游客户端关闭连接时退出。

### `codex resume`

通过 ID 继续一个交互会话或恢复最近的对话。 `codex resume` 将 `--last` 限定到当前工作目录，除非您传递 `--all`。它接受与 `codex` 相同的全局标志，包括模型和沙盒重写。

| 键 | 类型 / 值 | 详细信息 |
| --- | --- | --- |
| `--all` | `boolean` | 选择最近会话时包括当前工作目录之外的会话。 |
| `--last` | `boolean` | 跳过选择器，自动恢复当前工作目录的最近对话。 |
| `SESSION_ID` | `uuid` | 恢复指定的会话。省略并使用 `--last` 继续最近的会话。 |

键

`--all`

类型 / 值

`boolean`

详细信息

选择最近会话时包括当前工作目录之外的会话。

键

`--last`

类型 / 值

`boolean`

详细信息

跳过选择器，自动恢复当前工作目录的最近对话。

键

`SESSION_ID`

类型 / 值

`uuid`

详细信息

恢复指定的会话。省略并使用 `--last` 继续最近的会话。

### `codex fork`

将先前的交互会话分叉到新线程。默认情况下，`codex fork` 打开会话选择器；添加 `--last` 自动分叉您最近的会话。

| 键 | 类型 / 值 | 详细信息 |
| --- | --- | --- |
| `--all` | `boolean` | 在选择器中显示当前工作目录以外的会话。 |
| `--last` | `boolean` | 跳过选择器，自动分叉最近的对话。 |
| `SESSION_ID` | `uuid` | 分叉指定的会话。省略并使用 `--last` 分叉最近的会话。 |

键

`--all`

类型 / 值

`boolean`

详细信息

在选择器中显示当前工作目录以外的会话。

键

`--last`

类型 / 值

`boolean`

详细信息

跳过选择器，自动分叉最近的对话。

键

`SESSION_ID`

类型 / 值

`uuid`

详细信息

分叉指定的会话。省略并使用 `--last` 分叉最近的会话。

### `codex sandbox`

使用沙盒助手在 Codex 内部使用相同的策略运行命令。

#### macOS seatbelt

| 键 | 类型 / 值 | 详细信息 |
| --- | --- | --- |
| `--config, -c` | `key=value` | 将配置重写传递到沙盒运行中（可重复）。 |
| `--full-auto` | `boolean` | 授予对当前工作区和 `/tmp` 的写入权限，无需批准。 |
| `COMMAND...` | `var-args` | 在 macOS Seatbelt 下执行的 shell 命令。所有在 `--` 之后的内容被转发。 |

键

`--config, -c`

类型 / 值

`key=value`

详细信息

将配置重写传递到沙盒运行中（可重复）。

键

`--full-auto`

类型 / 值

`boolean`

详细信息

授予对当前工作区和 `/tmp` 的写入权限，无需批准。

键

`COMMAND...`

类型 / 值

`var-args`

详细信息

在 macOS Seatbelt 下执行的 shell 命令。所有在 `--` 之后的内容被转发。

#### Linux Landlock

| 键 | 类型 / 值 | 详细信息 |
| --- | --- | --- |
| `--config, -c` | `key=value` | 在启动沙盒之前应用的配置重写（可重复）。 |
| `--full-auto` | `boolean` | 授予对当前工作区和 `/tmp` 在 Landlock 沙盒中的写入权限。 |
| `COMMAND...` | `var-args` | 在 Landlock + seccomp 下执行的命令。在 `--` 后提供可执行文件。 |

键

`--config, -c`

类型 / 值

`key=value`

详细信息

在启动沙盒之前应用的配置重写（可重复）。

键

`--full-auto`

类型 / 值

`boolean`

详细信息

授予对当前工作区和 `/tmp` 在 Landlock 沙盒中的写入权限。

键

`COMMAND...`

类型 / 值

`var-args`

详细信息

在 Landlock + seccomp 下执行的命令。在 `--` 后提供可执行文件。

## 标志组合和安全提示

- 设定 `--full-auto` 进行无人值守的本地工作，但请避免与 `--dangerously-bypass-approvals-and-sandbox` 组合，除非您在专用沙盒 VM 中。
- 当您需要授予 Codex 对更多目录的写入访问权限时，优先使用 `--add-dir`，而不是强制使用 `--sandbox danger-full-access`。
- 将 `--json` 与 `--output-last-message` 一起使用时，可以在 CI 中捕获机器可读的进度和最终的自然语言摘要。

## 相关资源

- [Codex CLI 概述](https://developers.openai.com/codex/cli)：安装、升级和快速技巧。
- [配置基础](https://developers.openai.com/codex/config-basic)：持久化默认值，例如模型和提供者。
- [高级配置](https://developers.openai.com/codex/config-advanced)：配置文件、提供者、沙盒调优和集成。
- [AGENTS.md](https://developers.openai.com/codex/guides/agents-md)：Codex 代理能力和最佳实践的概念概述。
